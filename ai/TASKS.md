# TASKS

Last updated: 2026-09-25 · Format: `docs/standards/ID_STANDARDS.md`

**Filing rule (CHG-005):** every task sits under the heading of the phase or milestone whose scope it delivers — not under the phase that happened to be current when it was opened. Work delivered ahead of its phase is listed in `ai/MASTER_ROADMAP.md` → "Work delivered ahead of its phase". `npm run records` fails if a task is not under a recognised heading or if a task id is duplicated.

## PHASE 00 — Project Bootstrap & AI Infrastructure

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0001 | Initialize local Git repository (`main`) and add GitHub remote | T3 | DONE | — | Remote: `github.com/erenkarakoc/geoges-panel` (private) |
| TASK-0002 | Create `AGENTS.md`, `CLAUDE.md`, `README.md`, `.gitignore` | T3 | DONE | TASK-0001 | Owner approved 2026-09-15 |
| TASK-0003 | Create `/ai` state system | T2 | DONE | TASK-0001 | Owner approved 2026-09-15 |
| TASK-0004 | MASTER_ROADMAP with phase gates | T2 | DONE | TASK-0003 | Approved 2026-09-15; Milestone M1 added (CHG-001) |
| TASK-0005 | Standards: ID, naming, quality gates, git workflow | T2 | DONE | TASK-0003 | Owner approved 2026-09-15; `GIT_WORKFLOW.md` records Phase 00 direct-to-`main` exception, branches from Phase 01 |
| TASK-0006 | ADR-001…ADR-013 | T2 | DONE | TASK-0003 | Owner approved 2026-09-15; ADR-013 stale status corrected |
| TASK-0007 | Skill audit: sources, commits, scripts, licenses, risks | T2 | DONE | TASK-0003 | Results in `ai/AI_SKILLS.md` |
| TASK-0008 | Vendor approved skills into `.claude/skills/` at pinned commits | T1 | DONE | TASK-0007 | 8 skills; no executable content except Tailwind sync script (not run) |
| TASK-0009 | Disable `ui-ux-pro-max` for this project | T3 | DONE | TASK-0003 | `.claude/settings.json` |
| TASK-0010 | Enable claude-mem for this project (local-only) | T1 | DONE | TASK-0007 | 2026-09-15: first verification FAILED (plugin not installed for this project path). Installed v13.11.0 (`f5633c1`) at project scope with owner approval; DB backed up first. Observation generation failed (`OAuth session expired and could not be refreshed`; CLI refresh token expired 2026-08-02); owner re-logged in the Claude Code CLI. Fresh-session verification PASSED (session 68): SessionStart context injected, worker healthy (v13.11.0, PID 9652), observations #325–#328 stored, 0 errors/auth errors, telemetry `false`, Chroma local, no cloud sync. Follow-up: login expiry (see TASK-0018) |
| TASK-0018 | Prevent silent claude-mem stop when the Claude login expires (2026-10-15) | T2 | TESTING | TASK-0010 | Owner chose option 1 (long-lived `claude setup-token`). 2026-09-15: owner set user env var `CLAUDE_CODE_OAUTH_TOKEN` (presence/format checked, value never read). Worker (PID 9652, started 17:30) not yet respawned, so the token is unused until Windows restart. Owner deferred verification; pending: after restart confirm new worker PID, observations stored, 0 auth errors. Scheduled reminder (option 2) not chosen. 2026-09-15 23:25 Windows restart: new worker PID 19196 (23:27), env var present, 0 error/auth lines, observations #403–#405 stored. Limitation: `~/.claude/.credentials.json` is still valid, so the env-var fallback path is not yet exercised; re-check after 2026-10-15 |
| TASK-0019 | Stop console window flashes caused by claude-mem on Windows | T2 | DONE | TASK-0010 | Root cause (process trace 2026-09-15): detached worker `bun.exe` spawns `git` without `windowsHide`, so Windows opens Windows Terminal briefly. Owner chose a local patch: added `windowsHide:!0` to 2 git calls in `worker-service.cjs` and 1 each in `context-generator.cjs`, `transcript-watcher.cjs` (v13.11.0 cache); `node --check` OK; originals in `~/.claude-mem/backups/worker-service-pre-windowsHide-2026-09-15/`. Takes effect after Windows restart; lost on plugin update. Verified 2026-09-15 23:42 after restart: patch present, worker (PID 19196) spawned `git` twice with 0 Windows Terminal/OpenConsole launches (before: one per git call) |

## PHASE 01 — Requirements & Domain Analysis

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0020 | Glossary confirmation rounds (OQ-007) | T2 | DONE | TASK-0014 | Round 1 (2026-09-15): D-027 Party with roles, D-028 fire/zayi/hurda, D-029 guarantee types, D-030 subcontractor payment methods. Round 2: D-031 panel course, D-032 single lug type, D-033 one net party balance, D-034 per-currency balances. All OPEN terms closed; PROPOSED terms continue per module 2026-09-19: owner confirmed all 219 PROPOSED terms in four rounds of grouped questions (15 groups); 234 terms CONFIRMED, none PROPOSED or OPEN. |

## CHG-005 — Record consistency and deterministic guards

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0038 | Record consistency fixes and deterministic guards (CHG-005) | T2 | DONE | — | Committed `ab0f477` on `infra/chg-005-record-consistency`; the pre-commit gate ran and passed on its own commit. Owner approved the three judgment calls (M1 redefinition, `PARTIALLY_DONE` phases, TASK-0027 block) 2026-09-18. Self-review done: validator passes in both modes, journal hook verified firing live, resume hook pipe-tested, `npm run check` passes except a pre-existing defect outside this task (see KNOWN ISSUES). Nine contradictions found and fixed in the state system (stale roadmap, dead Milestone M1, mis-filed tasks, stale "Last updated" stamps, `docs/sources/` deletion hazard (folder removed 2026-09-19), REQ-WFL-011, REQ-WFL-028 filed under REQ-NFR, stale branch line, `CODE ALLOWED` contradicting shipped code). Guards added: `scripts/check-records.mjs` + `npm run records` wired into `npm run check`, versioned pre-commit hook in `.githooks/`, hook-written session journal for mid-session handover, PROJECT_RULES §9, §13, §21 amendments. D-072…D-076 **Owner approved 2026-09-18.** |

## PHASE 02 — UX, Information Architecture & User Flows

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0048 | Screen inventory: every screen, the requirements it carries, default roles | T2 | DONE | — | Phase 02 item 1. `docs/ui-ux/SCREEN_INVENTORY.md`: 105 screen rows (104 active, data import deferred) in the menu groups of CHG-004, each with type, address, requirements and roles; a script confirms all 438 requirements are on a screen or listed as screen-less (8 platform rules); the 26 management questions of REQ-NFR-004 each mapped to a screen. Placements D-217; flow designer and record-type builder left to the D-108 round. `SCR-<NNN>` added to the ID standard. Confirmed by the owner 2026-09-19; a "Raporlar" entry (SCR-017) added to the work layer (D-218) |
| TASK-0049 | List, detail and form patterns | T2 | DONE | TASK-0048 | Phase 02 item 3. `docs/ui-ux/SCREEN_PATTERNS.md`: the three patterns mapped to installed COSS components, shared rules (feedback, permissions, phones), behaviour and states for each; three owner choices (D-219). No custom component needed. Confirmed by the owner 2026-09-19 |
| TASK-0050 | Daily site log screen (SCR-021) | T2 | DONE | TASK-0049 | Phase 02 item 4. `docs/ui-ux/screens/SCR-021-daily-site-log.md`: layout, two views, the 15 sections with their rules, behaviour, states, COSS components (no custom component). D-220 closes OQ-027. Confirmed by the owner 2026-09-19 with its derived details |
| TASK-0051 | Per-screen state matrix | T2 | DONE | TASK-0050 | Phase 02 item 2. `docs/ui-ux/SCREEN_STATES.md`: common rules for loading, error, partial, no permission, no connection, irreversible-action confirmation; per screen the empty-state text and action and the special states, for the 103 active screens outside SCR-021. Confirmed by the owner 2026-09-19 with D-221 |
| TASK-0052 | Administration page and flow designer UX (D-108 round) | T2 | DONE | TASK-0042 | Phase 02 item 6. `docs/ui-ux/ADMINISTRATION.md`; SCR-195…198 added, SCR-192 moved to Onaylar, admin addresses under `/admin`; D-223. Confirmed by the owner 2026-09-19 |
| TASK-0053 | Accessibility targets (WCAG 2.2 AA) | T2 | DONE | TASK-0052 | Phase 02 item 7. `docs/ui-ux/ACCESSIBILITY.md`: project rules per WCAG criterion, target sizes, automatic-only testing, focus ring finding; D-225. Confirmed by the owner 2026-09-19 |
| TASK-0054 | Apply the light-theme focus ring token and translate COSS sidebar's English accessible names | T2 | DONE | TASK-0053 | 2026-09-21, no COSS file edited. Light theme `--ring` neutral-600 (D-225). Measured on the dev server: COSS draws the outline of elements without their own focus style (plain links) at 50% of `--ring`, which stayed at 2.27:1 even with the new token, so the light theme's base-layer outline now uses the full token; component utilities still win. Input border, button ring and link outline all measure 6.8:1; the dark theme is unchanged. The sidebar rail gets the Turkish name and tooltip "Menüyü aç veya kapat" through its props. COSS's English-titled mobile sheet can no longer open: at phone widths Ctrl/Cmd+B is stopped in the capture phase (mechanism verified in the browser; other shortcuts unaffected). Deviations logged in DESIGN_SYSTEM_RULES §4.1 rows 19a and 19d. The signed-in shell was not opened (no password entry by the agent) |
| TASK-0055 | Custom-element list | T2 | DONE | TASK-0053 | Phase 02 item 8 (roadmap deliverable). `docs/ui-ux/CUSTOM_ELEMENTS.md`: particle figure (D-047), charts everywhere (D-066, D-226), flow diagram canvas (D-224); chart rules. Confirmed by the owner 2026-09-19 |
| TASK-0056 | Layout and COSS components of the special screens | T2 | DONE | TASK-0055 | Phase 02 acceptance ("every screen lists states and COSS components"). `docs/ui-ux/SPECIAL_SCREENS.md` covers the Ö, P, S and Ç screens not specified elsewhere; no new custom element. Confirmed by the owner 2026-09-20 |
| TASK-0057 | Module boundaries, public APIs and the capability contract format | T2 | DONE | — | Phase 03. Per-module boundary spec (what it owns, what it exposes, what it may call), the contract format of capability catalogs and the contract tests that break CI when code and declaration drift (D-078). Written 2026-09-20: `docs/architecture/MODULE_BOUNDARIES.md` — the three surfaces a module exposes, folder layout, synchronous-vs-event rule, read models for reporting (D-233), contract tests, per-module boundary table and what counts as a violation. Follow-up for Phase 07: relax the lint rule from "no cross-module import" to "only through index.ts". Confirmed by the owner 2026-09-20 |
| TASK-0058 | Event backbone architecture | T1 | DONE | TASK-0057 | Phase 03. Outbox, dispatch, ordering, idempotency, retries and dead letters; which events are synchronous; how a flow instance subscribes (REQ-WFL-007, D-104). Written 2026-09-20: `docs/architecture/EVENT_BACKBONE.md` (D-234). Confirmed by the owner 2026-09-20 |
| TASK-0059 | Workflow engine architecture | T1 | DONE | TASK-0058 | Phase 03. Definition model (JSON schema), versioning (D-086, REQ-WFL-024), execution and step ownership (D-097), three-outcome approvals (D-099), for-each (D-096), record node (D-095), windowed-condition safeguards (D-100), trial run (REQ-WFL-025), traceability (D-087), system authority (D-082). Written 2026-09-20: `docs/architecture/WORKFLOW_ENGINE.md` (D-235). Confirmed by the owner 2026-09-20 |
| TASK-0060 | Identity, permission and visibility architecture | T1 | DONE | TASK-0057 | Phase 03. Multi-role, scope, delegation, acting role, owner layer, data classes, row visibility and the RLS direction for Phase 04; D-083 enforced in code; password and session rules (D-230). Written 2026-09-20: `docs/architecture/PERMISSIONS.md` (D-236; 2FA recovery closes the REQ-IAM-003 gap). Confirmed by the owner 2026-09-20 |
| TASK-0061 | Configuration, rules and custom fields architecture | T2 | DONE | TASK-0057 | Phase 03. Effective-dated rules and catalogs (REQ-WFL-032, REQ-ADM), custom fields (REQ-ADM-010), feature flags, and how a catalog change never rewrites closed periods (REQ-PRF-010). Written 2026-09-20: `docs/architecture/CONFIGURATION.md` (D-237). Confirmed by the owner 2026-09-20 |
| TASK-0062 | Ports, adapters and cross-cutting services | T2 | DONE | TASK-0057 | Phase 03. Auth, storage (R2), jobs, notifications and push, exchange rate, weather, e-mail, PDF; data access approach (OQ-020); search architecture (TASK-0029); live updates limited to counters and the queue (D-232). Written 2026-09-20: `docs/architecture/PORTS_AND_SERVICES.md` (D-238, D-239, D-240; OQ-020 closed). Confirmed by the owner 2026-09-20 |
| TASK-0063 | Storage direction for user-defined record types | T1 | DONE | TASK-0059, TASK-0060 | Phase 03. The hardest CHG-006 question (RISK-010): how a user-defined type is stored, permission-filtered (D-092), searched, reported and changed without losing history (D-094). Decision here, schema in Phase 04, spike in Phase 06. Written 2026-09-20: `docs/architecture/RECORD_TYPES.md` (D-241). Confirmed by the owner 2026-09-20 |
| TASK-0064 | Phase 06 spike list | T2 | DONE | TASK-0058, TASK-0059, TASK-0060, TASK-0061, TASK-0062, TASK-0063 | Phase 03 exit deliverable. Every risky assumption becomes a named spike with a pass/fail question. Written 2026-09-20: `docs/architecture/spikes/README.md` — 16 spikes, each with its pass criterion and the decision it protects. Confirmed by the owner 2026-09-20 |
| TASK-0065 | Database conventions and migration strategy | T2 | DONE | TASK-0057 | Phase 04. `docs/database/CONVENTIONS.md`: naming, keys and common columns, money and quantity precision (D-243, D-244), time, status vs catalog, history channel, ledger rules, indexes, RLS on every table, expand-then-contract migrations, schema tests. Confirmed by the owner 2026-09-20 |
| TASK-0066 | Platform schema: IAM, AUD, DOC, WFL, TSK, ADM | T1 | DONE | TASK-0065 | Phase 04. Users, roles, assignments with scope, delegations; audit and field history; documents; flow definitions, instances and steps; tasks and notifications; catalogs, dated rules, calendar, exchange rates; outbox and search rows. Written 2026-09-20: `docs/database/SCHEMA-PLATFORM.md` — 45 original tables (previous note incorrectly said 34); 48 after D-247 across iam, aud, doc, wfl, tsk, adm and the shared core schema (outbox, deliveries, dead letters, scheduled jobs, search rows). Confirmed by the owner 2026-09-20 |
| TASK-0067 | Operations schema: PRJ, SIT, INV, PUR, FAC, EQP | T1 | DONE | TASK-0066 | Phase 04. Projects, walls, targets; daily site log and its sections; stock ledger, shipments, counts; suppliers and orders; factory logs and batches; assets, custody, maintenance, crane logs. Written 2026-09-20: `docs/database/SCHEMA-OPERATIONS.md` — prj, sit, inv, pur, fac, eqp; the daily site log split into its entry tables, the stock ledger as append-only with a rebuildable balance. Confirmed by the owner 2026-09-20 |
| TASK-0068 | Commercial and finance schema: CRM, QTE, FIN | T1 | DONE | TASK-0066 | Phase 04. Leads, companies, tenders; quotes, versions, sales orders; progress payments, income and expense ledger, party ledgers, payments, cash projection, period close. Written 2026-09-20: `docs/database/SCHEMA-COMMERCIAL-FINANCE.md` — crm, qte, fin; ledgers append-only, closed periods refuse new rows, separation of duties as a database constraint. Confirmed by the owner 2026-09-20 |
| TASK-0069 | Corporate schema: HR, CMP, QHS, MTG, SUP | T1 | DONE | TASK-0066 | Phase 04. Employees, timesheets, payroll, leave, checklists; contracts, obligations, guarantees, delay files; certificates, nonconformities, OHS incidents, trainings; meetings and decisions; support tickets. Written 2026-09-20: `docs/database/SCHEMA-CORPORATE.md` — hr, cmp, qhs, mtg, sup; sensitive employee columns isolated in their own table, medical reports reduced to existence and dates (D-186). Confirmed by the owner 2026-09-20 |
| TASK-0070 | Analytics schema: RPT read models, PRF, INT, STR | T2 | DONE | TASK-0067, TASK-0068, TASK-0069 | Phase 04. Event-fed read models (D-233), saved views; KPI catalog, scores, bonuses; recommendations and scenarios; targets, budgets, health scorecard. Written 2026-09-20: `docs/database/SCHEMA-ANALYTICS.md` — read models with rebuild markers plus the modules' own records (KPI catalog, bonuses, recommendations, budgets). Confirmed by the owner 2026-09-20 |
| TASK-0071 | User-defined record type schema | T1 | DONE | TASK-0066 | Phase 04. `custom_record`, type definitions, link table, generated indexes, the single RLS policy (D-241, ADR-016). Written 2026-09-20: `docs/database/SCHEMA-CUSTOM-RECORDS.md` — six tables, one RLS policy for every user-defined type, indexes generated from the definition. Confirmed by the owner 2026-09-20 |
| TASK-0072 | Phase 04 review: data coverage and naming | T2 | DONE | TASK-0066, TASK-0067, TASK-0068, TASK-0069, TASK-0070, TASK-0071 | Phase 04 exit. Every requirement with data maps to a table; every table has a scope column, an RLS policy and a history channel; naming review against `docs/standards/`. Written 2026-09-20: `docs/database/COVERAGE.md` — 212 original definitions across 26 schemas (previous total 211 corrected by TASK-0092); 215 after D-247, the requirements that legitimately have no table, the mandatory three checks per table and what stays open for later phases. Confirmed by the owner 2026-09-20 |
| TASK-0073 | Environment and operations model | T2 | DONE | TASK-0065 | Phase 05. `docs/infrastructure/ENVIRONMENTS.md`: local-first model, single Supabase, secrets, migrations, what changes at go-live, backup and restore as they stand today (D-245). Confirmed by the owner 2026-09-20 |
| TASK-0074 | CI and quality gate | T2 | DONE | TASK-0073 | Phase 05. `docs/infrastructure/CI.md`: the local pre-commit gate that already runs, the GitHub checks added in Phase 07 (schema, contract, accessibility, keyboard, contrast, build, dependency audit), test tiers, migration testing. Confirmed by the owner 2026-09-20 |
| TASK-0075 | Owner's local setup guide | T3 | DONE | TASK-0073 | Phase 05. `docs/infrastructure/LOCAL_SETUP.md` in Turkish for Windows; verified and updated when the first runnable slice exists. Confirmed by the owner 2026-09-20 |
| TASK-0076 | Reset commands and configuration export/import | T2 | DONE | TASK-0101 | Phase 07, D-246. Built 2026-09-22: every table registers its layer (`seed`, `config`, `business`, `system`) in `core.table_layer` from its own migration; the migration runner rolls back a migration that leaves a table unregistered, points a foreign key the wrong way between layers or gives a config/seed table no primary key. `db:reset:data` empties business tables and re-runs `db/seeds`; `db:reset:config` exports configuration first, asks for `SIFIRLA`, then empties business, config and seed tables and re-seeds; `db:sample` loads `db/samples`; `db:mark-real-data` (asks for `KILITLE`, no undo) locks both resets and the sample loader; `config:export` / `config:import` move config tables as JSON, refuse another migration head, write nothing on any conflict, keep identity values and advance their sequences. Migration 0002 (+ down). Tests: 17 unit, 11 real-database in a probe schema. CI run 35657140052 green (database job 29/29 before and after a full rollback). The owner dropped the test-period pre-migration dump (D-255): the runner now requires a dump only after `db:mark-real-data`. 0002 applied to the Supabase test project on 2026-09-22; local `npm run test:db` 29/29. Self-review done |
| TASK-0077 | Backup and disaster recovery plan | T1 | DONE | TASK-0073 | Phase 05. `docs/infrastructure/BACKUP_AND_RECOVERY.md`: what is protected, today's local-period routine (dump before every migration), the five things required before real data (off-site backup, hourly copies, a timed restore drill, file replication, secret recovery), eight failure scenarios with target times, and what must be done after any recovery. Confirmed by the owner 2026-09-20 |
| TASK-0078 | Operational runbooks | T2 | DONE | TASK-0077 | Phase 05. `docs/infrastructure/RUNBOOKS.md`: eleven runbooks — panel will not start, cannot sign in, queue stopped, dead letters, failed flow instance, bad migration, wrong data entered, missing exchange rate, file problems, leaked key, lost owner access. Confirmed by the owner 2026-09-20 |
| TASK-0042 | The eight end-to-end flows (REQ-WFL-011, REQ-WFL-028) as real flow definitions, tested step by step against the node palette | T2 | DONE | TASK-0041 | CHG-006, D-089, D-104. Each end-to-end process written as short flows that trigger each other; each step marked core / palette / gap. Redo of the 2026-09-17 palette test (`docs/workflows/README.md`) with the new nodes (D-095, D-096, D-099, D-102); no gap may remain. The definitions become the engine's acceptance tests in Phase 08. Done 2026-09-19: `docs/workflows/END_TO_END_FLOWS.md` — 8 processes as chained flows, no palette gap; 4 catalog gaps and one linking rule closed by D-222. Confirmed by the owner 2026-09-19 |

## PHASE 06 — Validation Spikes

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0079 | SPIKE-07: flow diagram canvas | T2 | DONE | TASK-0064 | Phase 06. PASS 2026-09-20: `docs/architecture/spikes/SPIKE-07-flow-canvas.md`. 40 steps render in 258 ms, 12.1 ms median frame while zooming and panning, no horizontal scroll at 375 px, every node keyboard-reachable; `@xyflow/react` (MIT, ~88 KB gzip) confirmed as the library behind the custom canvas (D-224). Three notes carried to Phase 08: one tab stop with arrow-key navigation, mini-map off on phones, lazy-load the library |
| TASK-0080 | SPIKE-10: PDF quality | T2 | DONE | TASK-0064 | Phase 06. 2026-09-21: PASSED with the production font. Root cause of the old fallback found: next/font emits one woff2 per subset with unicode-range, and the spike had embedded only the preloaded `latin` file, so g-breve, s-cedilla and dotted capital I fell back to Times (predicted and confirmed 6/6); the app's own config already requests `latin` + `latin-ext`. Both documents were regenerated with those two Geist files and no system or proprietary font at all, headers/footers included, and all four pages were inspected (Geist shifts the daily report break from 25 to 23 rows on page 1; continuity intact, 34 rows). The earlier "full TTF required" condition and the same-day "shattered text layer" finding are both RETRACTED: with positional joining every probe word is found in all four PDFs; the naive space-joining extractor was the defect. Carried rules: templates load both subset files with unicode-range, header/footer templates get their own @font-face, the archive indexer joins text by position, layout is verified with the production font, and Geist embeds as Type3 (test a static instance if an archival format is ever required). Linux Chromium size stays a hosting input under DEF-008. `docs/architecture/spikes/SPIKE-10-pdf-quality.md` |
| TASK-0081 | SPIKE-11: TCMB exchange rate | T2 | DONE | TASK-0064 | Phase 06. 2026-09-21: the three reopened behaviours are now covered by 30 automated checks (`tcmb3.mjs`). Live: the dated file's `Tarih` matches the requested business day, both date formats coexist, JPY carries `Unit=100`, a weekend dated file returns 404, and trap 1 was caught live — at 02:30 on Monday 21.09 `today.xml` served the 18.09 bulletin while the 21.09 dated file 404'd. Simulated: exactly one `exchange_rate.missing` per day after four attempts at 10/30/60-minute intervals, pending amounts marked "kur bekliyor", approved records untouched (REQ-ADM-015), automatic completion with correct USD and Unit-divided JPY conversion, and idempotency on repeat runs. Two corrections recorded: the test's own UTC date bug (business-day maths must never use UTC) and the retry-interval reading, left as an operations parameter. LIMIT: the state machine runs in memory; real persistence, queue, event bus and concurrency belong to Phase 07 adapter tests. `docs/architecture/spikes/SPIKE-11-tcmb-exchange-rate.md` |
| TASK-0082 | SPIKE-01: RLS correctness and least privilege | T1 | DONE | TASK-0064 | Phase 06. Twelve assertions passed after correcting broad fixture grants; first self-grant exploit reproduced and rolled back. Report: `docs/architecture/spikes/SPIKE-01-rls-correctness.md`. Product IAM and TLS limitations remain explicit |
| TASK-0083 | SPIKE-02: RLS query performance | T1 | DONE | TASK-0082 | Phase 06. 114,977 logs and 115,015 movements; 20 samples per query; three-trip parameterized list/detail/stock transaction p95 239–240 ms. Report: `docs/architecture/spikes/SPIKE-02-rls-performance.md` |
| TASK-0084 | SPIKE-03: atomic ordered outbox | T1 | DONE | TASK-0064 | Phase 06. Ten checks pass: atomic source/event rollback, process exit before commit, 10,000 events in record order, pause/restart, duplicate suppression. Report: `docs/architecture/spikes/SPIKE-03-outbox-reliability.md` |
| TASK-0085 | Repair stale resume records and enforce phase pointers | T2 | DONE | TASK-0064 | Phase 06. CURRENT_STATE and SESSION_HANDOFF must each identify the single IN_PROGRESS roadmap phase; negative tests cover stale and absent pointers and ambiguous active phases. No product code |

| TASK-0086 | SPIKE-04: pinned workflow version and persistent trace | T1 | DONE | TASK-0064 | Phase 06. Seven checks pass: fresh-process resume on v1 after v2 publication, separate version traces, published-edit and stale-trial rejection. Report: `docs/architecture/spikes/SPIKE-04-workflow-versioning.md`. No product code |
| TASK-0087 | SPIKE-05: shared dry-run execution without effects | T1 | DONE | TASK-0064 | Phase 06. Ten checks pass across approve/reject/return/false-condition paths; read-only write rejection and no persistent changes. Thirteen independent path/owner assertions shared with SPIKE-04. Report: `docs/architecture/spikes/SPIKE-05-dry-run-parity.md`. No product code |
| TASK-0088 | SPIKE-06: bounded historical conditions | T1 | DONE | TASK-0064 | Phase 06. Nine checks pass: 500k history, fresh count/sum, p95 238–241 ms, genuine 2-second statement timeout, failed-instance trace and notification intent without business effects. Report: `docs/architecture/spikes/SPIKE-06-windowed-conditions.md`. No product code |

| TASK-0089 | SPIKE-08: custom record types end to end | T1 | DONE | TASK-0064 | Phase 06. 48 checks including two reproduced/fixed fixture flaws; 50,001 records, shared RLS, typed input, links, search/report, field retirement and definition history. List p95 240 ms, numeric filter p95 270 ms. Report: `docs/architecture/spikes/SPIKE-08-custom-record-types.md`. Data spike only, no UI/product code |

| TASK-0090 | SPIKE-12: Turkish search, scoped results and 500k-row performance | T1 | DONE | TASK-0064 | Closed 2026-09-21 under D-248 / CHG-008. Correctness and RLS criteria passed (73 checks plus 19 request/security checks earlier, 19 read-only post-maintenance checks today); warm searches stay under 300 ms (18 series, max 190 ms). First requests after instance idle are exempt by owner decision; the seven cold observations are kept. Historical initial failure in `docs/architecture/spikes/SPIKE-12-turkish-search.md`; current report `docs/architecture/spikes/SPIKE-12-search-retry.md` |
| TASK-0091 | Revise and revalidate RLS-preserving search queries and indexes | T1 | DONE | TASK-0064 | D-247 folded. Pinned transaction captured a genuinely new backend (age 0.157 s): first search 540/465 ms total/server, second 112/39 ms; 12 result/role/identity checks passed. A separate new-backend profile was fast (125/48 ms), so startup alone is not a proven cause. Original path retained; replay includes earlier 392/529/636 ms and new 540 ms failures. Equivalent-wrapper profiling reproduced 432/354 ms distributed across stages; 24 plan-mode cases offered no fix, and wait sampling was warm/inconclusive. Cold original wait trace now captured 571/487 ms without sampled PG waits. 2026-09-21: the range candidate FAILED cold acceptance (554/469 ms after ~6 h idle vs the original's 571/487 ms). New-backend startup is ruled out — six held backends aged 0.084–0.165 s first-searched in 40–48 ms and warm-up arms added nothing (24 checks). Controlled warm A/B gives the candidate only 0.5–1 ms and nothing on the heavy case. Plan probes: the covering index and the range rewrite fix the same planner choice, but the rewrite reaches the existing primary key with no extra index, while the index costs 15.05 MiB and ~45% more on record_count updates, so it is not recommended. Write-cost test bloat was repaired with VACUUM ANALYZE + REINDEX (sizes restored, 500,411 rows intact); the fresh statistics are a recorded baseline change. Covering index dropped; range functions kept. Cold triage done 2026-09-21 after ~10 h idle: no penalty on `select 1` or pure CPU, ~9x on the first scan of cached-looking pages (517 vs 58 ms, zero reads) and on search (383 vs 40 ms); the cost is outside the query. Owner granted a cold-start exception (D-248 / CHG-008); warm limit unchanged. Self-review: 19/19 read-only post-maintenance checks and the reclassified speed gate PASS (warm max 190 ms, seven cold observations excepted). DONE 2026-09-21. Original functions retained. `docs/architecture/spikes/SPIKE-12-search-retry.md`; no product code |
| TASK-0093 | SPIKE-14: read-model rebuild from a year of events | T1 | DONE | TASK-0064 | Phase 06, 2026-09-21. 21/21 checks on 500,000 events spread over one year: site summary and stock balance match the sources with 0 differences; the non-replayable notification subscriber was never called and 30,952 notifications stayed unchanged (negative control proves the probe works); drift in the live model detected and audited; shadow rebuild while readers kept seeing the old complete version, then atomic switch; duplicate and out-of-order delivery changed nothing; 325 later events caught up in 588 ms; scope RLS and write/execute denial (42501). Rebuild 19.3 s plus 2.1 s compare, 0.15% of the 4 h RTO; flat 50k-event chunks support a linear estimate of ~58 s for 1.5M and ~4.8 min for five years (estimates, not measured). The first 1.5M-event attempt filled the Free Plan test disk and rolled back cleanly. Fixture tables were UNLOGGED; measured read-model tables were logged. Task-producing subscriber not separately modelled. `docs/architecture/spikes/SPIKE-14-read-model-rebuild.md` |
| TASK-0094 | SPIKE-09: R2 signed links with permission checks | T1 | DONE | TASK-0064 | Phase 06, 2026-09-21. 17/17 checks against the `geoges-panel` R2 bucket with a key scoped to object read/write on that bucket. The link service reads the document under the caller's RLS scope and signs only what the caller can see: other-site, no-scope and anonymous requests got no link. Unsigned object/list requests (400), a renamed key or extended expiry inside a signed link (403) and an expired 5 s link (403 ExpiredRequest) were refused. A 50 MB file downloaded intact (sha256) with a Turkish RFC 5987 filename. Throttled to 512 KB/s, a download outlived its 20 s link and completed; resuming the cut download needed a fresh, re-authorised link plus a Range request (206). Link TTL can stay short; exact TTL is a product setting. Metadata lived in a never-committed transaction and test objects were deleted. Limits: simulated field network; public r2.dev access must be confirmed off in the dashboard; bucket is not EU-jurisdiction (RISK-001 decision before real personnel files). `docs/architecture/spikes/SPIKE-09-r2-signed-links.md` |
| TASK-0095 | SPIKE-13: live signal channel under weak connections | T2 | DONE | TASK-0064 | Phase 06, 2026-09-21. Local SSE server plus real headless Chrome, two users with different site scopes. The first run FAILED C5: during a 10 s offline window the open stream still delivered three signals, but each follow-up count request failed, and with no reconnect afterwards the counter stayed wrong for ~18 s. Fixed by retrying failed count requests with backoff and refreshing on the browser `online` event; the failing evidence is kept. After the fix 12/12: counter updates, no cross-scope signal, silent recovery after server drop (0.8 s), offline (62 ms after online), network-error outage (2.8 s) and slow 400 ms link; a 503 permanently closes the native EventSource, so our backoff wrapper reconnects (4.3 s); reload always shows the source count; every frame carries only the signal type. Limits: no proxy/CDN buffering, no multi-tab HTTP/1.1 connection cap (use HTTP/2 or one shared stream), signals not yet wired to the outbox worker, no real mobile network. `docs/architecture/spikes/SPIKE-13-live-signal-channel.md` |
| TASK-0096 | SPIKE-15: field photo upload on a weak connection | T2 | DONE | TASK-0064 | Phase 06, 2026-09-21. Real headless Chrome, online-only frame (DEF-002). A synthetic 4032x3024 photo was resized in the browser to 1600x1200 (5.86 MB -> 238 KB) and uploaded in 64 KB offset-checked chunks through the app server to R2 over a simulated 256 kbit/s link with an 8 s offline window. 11/11: the upload resumed from the server-reported offset with 0 bytes resent, the R2 object matched the resized photo byte for byte, the draft autosave retried and kept the value changed while offline, and a reload restored the form and the photo. Log findings: the chunk in flight at the cut had reached the server, so the offset must come from the server; the server-side drop was absorbed by Chrome's own retry of a reused connection, so it did not exercise our resume logic; the `online` event should cut the backoff short (upload resumed 7.5 s after reconnect). Limits: closing the tab while offline loses unsaved input (DEF-002 boundary); no EXIF/HEIC/real camera; simulated network. `docs/architecture/spikes/SPIKE-15-field-photo-upload.md` |
| TASK-0097 | SPIKE-16: in-house OCR of scanned documents | T2 | DONE | TASK-0064 | Phase 06, 2026-09-21. Local Tesseract (tesseract.js 7.0.0, tur+eng, downloaded with owner permission); no document left the machine. Synthetic dispatch note and weighing slip at three qualities, measured as searchability with the search normalisation (D-247). Realistic scan (150 dpi, tilt, noise, JPEG): dispatch note 10/10 search terms (raw 9/10), weighing slip 9/9, Turkish diacritics exact; net weight extractable; ~1 s per page for two passes. Poor scan (100 dpi, faded): 6/10 and 8/9, to be handled as a low-confidence quality flag and rescan prompt, not an outside service, so the owner decision point was not needed. Harness changed three times after seeing failures, all applied uniformly with raw results kept: dark default background (harness bug), a second sparse-text pass because bordered table rows were skipped, and a digit-punctuation spacing cleanup. Risk of fitting a small set: validate with real anonymised scans in Phase 07. `docs/architecture/spikes/SPIKE-16-ocr-in-house.md` |
| TASK-0098 | SPIKE-17: architecture boundary enforcement in CI | T2 | DONE | TASK-0064 | Phase 06, 2026-09-21. Added after the owner chose to test the roadmap candidate that had never become a spike. Throwaway fixture checked with the repo's own ESLint 9 and eslint-plugin-boundaries 7.2.0 (no new download). 14/14: 34 module edges read automatically from MODULE_MAP, graph acyclic; another module usable only through `index.ts` along a graph edge; deep, relative, type-only, dynamic-import and re-export bypasses and against-graph dependencies all blocked; CI fails on violation; a static SQL scan caught one module reading another's schema without false alarms; the real repo is clean under today's rule. Found a record inconsistency: MODULE_BOUNDARIES section 3 says QTE calls PRJ synchronously while MODULE_MAP draws that edge dashed. Limits: the SQL scan sees only literal strings (add per-module database grants); catalog contract tests belong to Phase 07. `docs/architecture/spikes/SPIKE-17-boundary-enforcement.md` |

| TASK-0092 | Enforce schema inventory counts against definitions | T2 | DONE | TASK-0072 | Corrected inherited 211 total vs 212 definitions and platform 34 vs 45. Current D-247 inventory is 215 / platform 48. Records gate rejects drift, duplicates and missing inventory. Six regression cases and real stale-total negative probe passed; self-review and full check passed (69 tests). Tooling only |

## PHASE 07 — Foundation Build

Plan: `docs/features/phase-07-foundation-plan.md`. Each T1/T2 task writes its implementation plan (PROJECT_RULES §10) before code. Carried from earlier headings: TASK-0028, TASK-0043, TASK-0054, TASK-0076.

| ID | Task | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0099 | Generate the ADR-001 boundary rule from MODULE_MAP and align module layout | T2 | DONE | — | 2026-09-21. `scripts/module-graph.mjs` generates the allowed module dependencies from MODULE_MAP (solid arrows direct, dashed arrows are events, every business module may use the six platform modules, platform modules never depend on business modules, no platform-to-platform arrows yet) and ESLint loads them; a cycle or an unreadable map fails the lint instead of loosening it. Modules reach another module only through `index.ts`; routes may use `index.ts` and `ui/`. New IAM `index.ts`; routes use it, and `auth/confirm` no longer reaches IAM infrastructure (behaviour unchanged). `npm run boundaries` scans module SQL for another module's schema. Tests: 15 real-config probe cases, 8 graph cases, 5 schema cases; repo lint clean, production build passes. Self-review done. Layout alignment: platform modules already live under `src/modules`; nothing else to move today |
| TASK-0100 | GitHub CI pipeline | T2 | DONE | TASK-0099 | `.github/workflows/ci.yml`: `npm ci` then `npm run check:commit` on Node 24 for every push to `main` and every pull request; full git history (the records gate compares stamps with commit dates and looks up deleted paths); actions pinned to commit SHAs (RISK-004); read-only token, no secrets. The extra CI.md section 2 steps arrive with the things they test. First run 35623138587 green on 2026-09-21: full history confirmed, records, schema-access, lint, tests and formatting all ran on GitHub. Self-review done |
| TASK-0101 | Data access foundation: migrations, restricted runtime role, transaction-local identity | T1 | DONE | TASK-0100 | ADR-015, CONVENTIONS.md, D-254; SPIKE-01/12 carry-forwards. Built 2026-09-22: `pg` + Kysely; SQL migrations with down files and a SHA-256 ledger (`npm run db:migrate`, `--status`, `--rollback-last`), pre-migration dump rule (`npm run db:backup`); restricted login role `geoges_app` (no RLS bypass, no role/db creation, no inheritance, no DELETE) whose password is generated locally and stored as a SCRAM verifier (`npm run db:app-role`); every connection verifies TLS against the pinned Supabase Root 2021 CA; `runAsUser` writes identity and statement timeout transaction-locally with bound parameters, rolls back on failure and destroys the connection if the rollback fails; `pg`, `kysely` and `@/platform/db` importable only from module data layers, SQL statements only there. 0001 applied to the test project. Tests: 46 unit (38 data layer and tools, 3 SQL location, 5 import rule), 18 real-database (runtime role over the pooler, RLS with and without identity, no leak across 40 concurrent transactions, rollback, injection, eight forbidden operations, uuid_v7); down file dry-run on Supabase inside a rolled-back transaction. New CI `database` job (empty PostgreSQL 17 with TLS: apply, re-run, test, roll back, apply, test): first run 35655520185 green on 2026-09-22, both database test rounds 18/18. Self-review done. Owner items: compare the pinned root's SHA-256 with the certificate downloaded from the Supabase dashboard; PostgreSQL 17 client tools for `db:backup` before real data (D-255 dropped the test-period dump) |
| TASK-0102 | Dynamic IAM: scoped roles, hierarchy, overrides, acting role, delegation, visibility | T1 | DONE | TASK-0101, TASK-0076 | PERMISSIONS.md, D-111…D-118, D-230, D-236; CHG-006: permission types and roles defined and assigned from the flow designer (D-098, D-101), flow design refused to roles without full visibility (D-083). Built 2026-09-22 (D-256): migration 0003 (`iam` schema, nine tables, RLS on all, guards for flow design D-083, the unrestricted owner layer, overlapping assignments and delegations of roles not held), factory role templates from PERMISSION_MATRIX (`db/seeds/0001_iam_role_templates.sql`), `npm run iam:bootstrap-owner`, portable configuration and sample-people removal in the reset tools, the permission service in `modules/iam` (snapshot per request, `assertCan`, `visibleColumns`, acting role, `managerOf`, `approvalOwner`). Tests: unit + 23 real-database tests; 0003 applied to the test project and its down file exercised. CI run 35664127779 green: database job 52/52 twice around a full rollback |
| TASK-0112 | Account security tables: 2FA recovery codes, sign-in lockout, session inactivity | T1 | DONE | TASK-0103, TASK-0105, TASK-0108 | Phase 07 IAM scope not covered by TASK-0025 (sequenced after audit, dated rules and notifications by D-257): ten one-time recovery codes stored as hashes and a manager-initiated second-factor reset (D-236), temporary lockout after failed sign-ins (REQ-IAM-005), 30-day session ended after 3 days of inactivity and at once on deactivation (D-230, REQ-IAM-006); `iam.recovery_code`, `iam.login_attempt`, `iam.session` (SCHEMA-PLATFORM). Also: the protected layout refuses a signed-in Supabase account without an active panel account (REQ-IAM-006/007 at the page level; the data is already refused by RLS since TASK-0102); roles requiring 2FA read from dated rules (REQ-IAM-003). `iam.user.must_setup_2fa` must follow the provider: cleared when a factor is verified, set again after a reset (today it is only written by `iam:bootstrap-owner`; the owner's flag was cleared by hand on 2026-09-22 after they set up panel 2FA). Implementation plan written 2026-09-24 (`docs/features/phase-07-foundation-plan.md`) with D-272 PROPOSED: five steps — migration 0038 for the three tables with definer functions callable before anyone is signed in; the lock refused before Supabase is asked, every attempt recorded, the lock audited; the panel's own session row beside Supabase's token for the 30-day and 3-day rules, touched at most every five minutes and revoked at once on deactivation; ten one-time codes whose use marks the panel session's `second_factor_at` instead of Supabase's `aal2`, the one place the gate widens; and the dated rule for roles requiring a second factor plus the page-level refusal of a Supabase account with no active panel account. The open question of its own row is answered in the plan: removing a lost factor needs Supabase's admin right, so `SUPABASE_SERVICE_ROLE_KEY` (already in `.env.local`, read by nothing today) goes into one server-only adapter for those two audited operations. D-272 ACCEPTED by the owner 2026-09-24, together with the two things it asked to be recorded (the server-only service-role adapter for removing a lost factor, and the lock releasing only with time); the build is under way, step by step. Step 1 done 2026-09-24 (0038 + the data layer + 11 database tests): the three tables with no privilege at all on the attempts and the code hashes, a select on sessions whose policy shows a person their own, definer functions for everything, the lock event audited without an actor, and a trigger that revokes every session the moment an account is disabled. 0039 then moved the settings inside those functions (`adm.rule_value`), which is D-272's one changed detail. Step 2 done the same day: the sign-in path refuses a locked e-mail before it asks the provider, records every try with its address and agent, and tells the try that crossed the line about the lock instead of a wrong password; the wording says the minutes left and never whether the address has an account (three unit tests). Step 3 done 2026-09-24: `iam.session` rows behind an httpOnly cookie, opened where a sign-in completes (both steps), revoked on sign-out, kept alive by reading it, and required by the one guard every action and query already went through. The routing rules gained the new state — mid-sign-in still goes to the second step, an ended panel session shows the sign-in form instead of bouncing between pages — with nine unit tests, and each session keeps a readable device label (three more tests). Everybody is signed out once by it. Not yet exercised in a browser: the signed-in half needs the owner to sign in. Step 4 part one done 2026-09-24: ten codes made at setup and shown once (readable alphabet, forgiving input, hashes only), the sign-in step offering "Telefonuma erişemiyorum", a spent code marking the panel session's own second factor — the single widening of the gate, in the routing rules with tests — the lost factor removed through one server-only service-role file for that one operation, and `must_setup_2fa` following the provider at last (0040, audited both ways). Step 4 finished the same day: `resetSecondFactorAction` removes another account's factors, asks that person for a new one, ends their sessions and is allowed only for `iam.module.manage` (checked in the function, not in a policy); 0041 publishes `two_factor.reset` to the outbox and TSK's new `tsk.security-alerts` subscriber notifies every owner, without a task and without marking it critical. Four more database tests. **No button yet:** "Kullanıcılar & Roller" has a menu entry and no screen, and Phase 02 never specified one, so the action waits for that screen rather than an invented one. Step 5 done the same day, finishing the task's five steps: 0042 reads the administrator's list of roles that must use a second factor (`iam.two-factor-roles`, seeded empty) and answers only about the person asking; combined with `must_setup_2fa` it sends somebody who owes a factor to set one up, from the panel and from the sign-in page alike. A provider account with no active panel account sees no page at all, and gets the form rather than a bounce on the sign-in page (REQ-IAM-006/007). Both rules are in the pure routing functions, eighteen unit tests. The rule test sets the list in a transaction it rolls back, because a rule row is immutable and a test must not change the environment's configuration. Browser pass 2026-09-24, partly done with the owner signed in: the panel's own session works end to end — two real rows, "Windows · Chrome" from the browser pane and "iPhone · Safari" from the owner's phone that morning, both with the second factor marked, a thirty-day expiry and a moving last-seen — and every page renders behind the new guard, with the two-factor screen in its manage view showing no recovery entry, as it should. Finished 2026-09-24 with the owner signing out and in: the recovery entry sits under the code step and opens to a field explaining that each code works once; a first enrolment shows ten distinct codes with the warning that they will not be shown again, verified without reading a single one (ten list entries matching the pattern, ten unused hashes in the database and a `two_factor.enrolled` audit entry); and five wrong passwords at a throw-away address produced the lock on the fifth, refusing the sixth with the same lock. Two faults the looking found and the tests could not: the recovery field's placeholder read like a real code, and a fifteen-minute lock announced itself as sixteen because the remaining time was measured against this machine's clock rather than the database's. Both fixed. DONE 2026-09-24 with the owner's word, its five steps built and seen working. Out of scope and recorded as such: a manual unlock, a device list, a "close every session" button. The manager's reset gets its button on the minimal users screen of D-273 |
| TASK-0103 | Audit log and record history | T1 | DONE | TASK-0102 | REQ-AUD, D-134 (nothing deleted), D-135 (audit screen for owners only). Built 2026-09-22 (D-258): migration 0004 (`aud.record_history`, `aud.audit_log`, history kind in the layer register, `core.column_data_class`, one history trigger, IAM event triggers, owner-only audit permission), seed `0002_aud_audit_log_permission.sql`, `setChangeReason` in `platform/db`, `modules/aud` (service, filters, SCR-193 at `/audit-log`), sign-in/sign-out events, reset and transfer events. Carried over: the "İşlem geçmişi" section of detail screens comes with the first detail screen (Phase 09); the approval, correction-request and document parts of REQ-AUD-001 with Phase 08, TASK-0109 and TASK-0107; SCR-193 adopts the shared standard list (saved views, density, card view, remembered filters) when that component is built with Phase 09; the delegation-end event needs the scheduler (TASK-0104). CI run for 563d026 green (database job 62/62 twice around a full rollback). Browser check with the owner signed in: SCR-193 lists the owner's own sign-in, filters and active-filter badge work, total and page numbers shown, phone layout works, no console errors; it showed the record type without whose record it was and labelled removed test accounts as "Sistem", fixed by migration 0005 (`target_name`) and a "Kaldırılmış hesap" label |
| TASK-0104 | Outbox, job runner, scheduler and read-model rebuild infrastructure | T1 | DONE | TASK-0101, TASK-0103 | ADR-014, EVENT_BACKBONE.md, D-233, D-234; SPIKE-03 ordered head-per-record selection and duplicate suppression; SPIKE-14 per-source-record rows, newer-event-only updates, shadow version with atomic switch, compare-and-audit, replayable subscribers only. Built 2026-09-22 (D-259): migration 0006 (outbox, subscriptions, deliveries, dead letters, scheduled jobs, read-model register, worker role `geoges_worker`, IAM catalog events, `iam.deactivate_departed`, `iam.publish_ended_assignments`), `publishEvent` / `scheduleJob` in `platform/db`, the worker in `platform/jobs` started from `src/instrumentation.ts`, `src/jobs/registry.ts`, `npm run jobs:status` / `jobs:retry` / `jobs:rebuild`. Tests: unit (retry schedule, Istanbul recurrence, registry) and 16 real-database tests including SPIKE-03's negative tests and the SPIKE-14 rebuild rules. Carried over: owner notification and system-problem task for dead letters and delay (TASK-0108), SSE signals (TASK-0108), first real read model (Phase 09), session end on departure (TASK-0112). CI run 35672376274 green (database job 79/79 twice around a full rollback). After restarting `npm run dev` the in-process worker planned and ran the two daily IAM jobs, no server errors |
| TASK-0105 | Catalogs, dated rules and typed custom fields | T1 | DONE | TASK-0102, TASK-0103, TASK-0104 | CONFIGURATION.md, D-237. Built 2026-09-22 (D-260): migration 0007 (`adm` schema: catalogs, catalog items, rule keys, append-only rules, custom field definitions; `core.fold_tr`; `adm.rule_value`, similar-item suggestions, merges with redirect and event, custom value check trigger; history records custom fields per field), seed `0003_adm_catalogs.sql`, `modules/adm` (`getRule`, `readRule`, catalog services, `visibleCustomFields`), feature switches (`FEATURES_OFF`, menu and pages). Carried over: SCR-190 and REQ-ADM-002…004 definitions (Phase 09 master data), custom fields in flow conditions (Phase 08), search (TASK-0110), lists and reports (their slices). CI run 35696301071 green. 2026-09-23 regression fix 0026: new rule insertion order resolves tied transaction timestamps without relying on UUID randomness; reversed-UUID test and all 10 ADM DB tests pass |
| TASK-0106 | Exchange rates and business-day calendar | T1 | DONE | TASK-0104, TASK-0105 | REQ-ADM-013…015, D-139, D-140; SPIKE-11: dated file with `Tarih` check, `Unit` division, local-time business days, one `exchange_rate.missing`, pending amounts completed. Built 2026-09-22 (D-261): migration 0008 (calendar, holidays, append-only exchange rates, fetch state; business-day, rate and fetch functions), seed `0004_adm_calendar.sql` (company calendar, 2026 public holidays, fetch rules), `ExchangeRateProvider` with the TCMB adapter (`platform/exchange-rates`), Istanbul date helpers (`platform/time`), job `adm.exchange-rates` (every 10 minutes), `isBusinessDay` / `addBusinessDays` / `rateFor` / `enterManualRate`. Tests: adapter against the real 18.09.2026 bulletin (no network in CI), 8 real-database tests (business days, site calendar, rate choice, manual rate, retries 10/30/60 and a single missing event, wrong-day bulletin, catch-up). Live: the dev server's worker fetched the real bulletins of 15-21 September (22 currencies each). Carried over: task and alert on a missing rate (Phase 08 flow, TASK-0108), completing waiting amounts (owning slices), calendar and rate screens (SCR-190, Phase 09), overtime rule contents (Phase 12). CI run 35718689116 green |
| TASK-0107 | Documents and storage: R2 adapter, signed links, resumable upload, OCR queue | T1 | DONE | TASK-0104 | ADR-003, REQ-DOC; SPIKE-09/15/16/10 carry-forwards; D-262. Real files only after the public r2.dev URL is disabled (ADR-003) and the jurisdiction decision (D-249) is taken. Step 1 built 2026-09-22: migration 0009 (`doc.document`, `doc.document_version`, `doc.upload`, `doc.extracted_text`, `doc.can_access`, guards, `doc.complete_upload`, `doc.archive_document`), seed `0005_doc_types.sql`, `platform/storage` (port, R2 and in-memory adapters), `modules/doc` (rules, data layer, service), composition root `src/records`, routes under `/api/documents`; 8 real-database tests (resumable upload with 409 and resume, site and commercial visibility, no link for a hidden version, versions and signed mark, no delete, archive reason and audit). Also closed a gap found on the way: server actions and route handlers accepted a session that had not passed its second factor (`signInIdentity` and the permission snapshot now require it). Steps 2-4 built the same day: migration 0010 (worker grants, `doc.record_bulk_download`), browser uploader logic (`modules/doc/ui/resumable-upload.ts`), `platform/text-recognition` (tesseract.js 7.0.0, pdfjs-dist 6.3.289, @napi-rs/canvas 1.0.9, models from `@tesseract.js-data/tur` and `/eng` 1.0.0), subscriber `doc.recognise-text`, hourly `doc.expired-uploads`, ZIP bulk download for a record or a project (client-zip 2.5.1) at `/api/documents/bulk`; audit labels for document, manual-rate and dead-letter events. Tests: unit (uploader 6, OCR on the SPIKE-16 weighing slip, PDF text layer and scanned page, ZIP names) and 12 real-database tests (adds recognition result, failure, expiry, bulk download visibility and audit); live R2 adapter test 2/2 (multipart, Turkish name, expired and altered link refused; skipped without `R2_` settings). After restarting the dev server the worker subscribed `doc.recognise-text` and ran `doc.expired-uploads`. Not yet shown end to end in a browser: shrinking a real photo, uploading it and reading its text need a record that carries documents; that walk-through happens with the first detail screen (Phase 09). CI run 35723626554 green (check and database jobs) |
| TASK-0108 | Notifications and tasks core | T2 | DONE | TASK-0102, TASK-0103, TASK-0104, TASK-0105 | REQ-TSK, D-130…D-133, ADR-018, D-252; SPIKE-13: SSE wrapper with backoff, data-request retry and `online` refresh, signal carries only its type; iPhone Home Screen step; e-mail digest against a local test mailbox until the provider (OQ-015). Plan approved 2026-09-22 (D-263). Step 1 built: migration 0011 (`tsk.task`, `tsk.escalation`, `tsk.notification`; scope check, visibility, give/complete/approve/reopen, one task per open problem, notify with a ten-minute repeat guard), `modules/tsk` (domain, data, service, server actions), SCR-013 `/tasks`, SCR-014 `/tasks/new` (opens as a Dialog on desktop and a Drawer on a phone over the current page through the intercepting route `@modal/(.)tasks/new`, owner request 2026-09-22; a direct visit shows the page), the task screen `/tasks/[taskId]` with history, the "Görev ver" header action; 8 unit and 12 real-database tests. Next: step 2 (notification drawer and SSE signals). Step 2 built: migration 0012 (notification events `notification.created` / `notification.read` carrying the recipient only, `tsk.task_audience`), `platform/signals` (signal contract, in-process hub, SSE response, browser wrapper with backoff, one stream shared across tabs, data refresher with retry), `/api/signals`, `/api/notifications`, subscriber `tsk.live-signals`, the bell SCR-015 (Popover on desktop, Drawer on a phone, unread counter, type filter as a Select at the owner's request, mark read). Tests: unit (signal contract, reconnect waits, refresher) and TSK database tests 13/13 including the live-signal audience. Browser check on localhost:3001 with the owner signed in: the counter rose live from 2 to 3 without a reload, two tabs updated together on one shared stream and both cleared when one marked all read; the desktop Popover, the Kritik filter and "Tümünü okundu say" work. It found two defects, fixed in the same step: the bell's refresher was stopped for good by React's development double mount, so the counter never updated, and a Web Lock granted after unmount connected and held the lock forever. On a phone width the Drawer opens with the right content but the browser pane does not paint its animation (same pane limit as step 1), so the phone view is still to be seen on a real phone. Test notifications "Deneme: …" stay read in the owner's list. Step 3 built: migration 0013 (`tsk.push_subscription`, save and expire for one's own browsers), `platform/push` (`PushSender` port, web-push 3.6.7 adapter, VAPID keys from server variables only, no keys without which phone notifications are simply off), subscriber `tsk.phone-push` (new task, approval request and critical alert; the panel's own words; an address the push service calls gone is retired), `/api/push`, `public/sw.js` (notifications only, nothing cached), `src/app/manifest.ts` for the iPhone Home Screen step (D-252), and the bell's switch with its refusal note. TSK database tests 16/16. Step 4 built: migration 0014 (`tsk.daily_digest`, `tsk.digest_for` counting a person's own morning and the company's yesterday for the owner layer, `tsk.people_without_digest`, `tsk.record_digest`), seed `0006_tsk_rules.sql` (`tsk.digest-time` 07:30, `tsk.escalation-wait-hours` 24), `platform/mail` (`MailSender` port; until the provider is chosen (OQ-015) messages are written as `.eml` files into `.mail/`, gitignored), job `tsk.daily-digest` every fifteen minutes (sends once the rule's hour has passed in Istanbul, nothing for a person with no work, one row per person and day), notification type `digest.daily`. MODULE_MAP gains `TSK --> ADM`, because the digest hour and the escalation wait are dated rules. Tests: unit (digest words, empty digest, owner's company line) and TSK database tests 17/17. Step 5 built: migration 0015 (doors `tsk.people_with_permission`, `tsk.owner_people`, `tsk.delegates_of`; `tsk.mark_overdue`, `tsk.next_escalation_target`, `tsk.escalate_task`, `tsk.tasks_needing_attention`, `tsk.system_health`), jobs `tsk.overdue-and-escalation` (every 15 minutes: tells the assignee once, then climbs assignee → active delegate → manager → owner layer, waiting the dated rule `tsk.escalation-wait-hours`) and `tsk.system-watch` (every 5 minutes: a dead letter or a queue more than 15 minutes behind opens one critical system-problem task for the owner layer and closes it when the cause clears), subscriber `tsk.exchange-rate-alarm` (a missing TCMB rate asks the people holding `adm.module.manage`, once, and closes when the bulletin arrives). TSK database tests 20/20; 0015's down file exercised. CI run 35769543866 green (check and database jobs). Carried over: the escalation chain and its waiting times move into the escalation flow (Phase 08); automatic tasks from business events come with the modules that publish them; the e-mail provider waits for OQ-015, so digests are written as files; the phone notification has not been seen on a real phone yet, and the `.mail/` digest has not been opened with the owner |
| TASK-0109 | Revision request core | T1 | DONE | TASK-0103, TASK-0108 | CONFIGURATION.md, REQ-ADM, D-141: approved records and closed periods change only through a revision request. Plan written 2026-09-23 (D-265, proposed): register of revisable record types, request with field/old/new and a reason, manager-then-owner default approver until the Phase 08 flow, old/new side by side, rejection needs a reason, the owning module applies an approved change through an applier registered in the composition root, corrections linked with `aud.revision_effect`; SCR-192 in the approvals screen. Steps 1-2 built 2026-09-23 (D-265): migration 0018 (`aud.revisable_record` register, `aud.revision_request`, `aud.revision_effect`; who may ask, who decides (requester's manager, else the owner layer), guards that keep a decided request and its fields fixed and delete nothing, `aud.submit_revision` / `decide_revision` / `mark_revision_applied` / `mark_revision_stale` / `record_revision_effect` / `revision_page`, events `revision_request.submitted` / `.approved` / `.rejected`), `modules/aud` (rules, data layer, service) and the applier register in the composition root `src/records`. Tests: unit (the change list, a stale request, the words of a value, the forms) and 7 real-database tests (a locked row refuses a direct change, only register fields may be asked for, one open request per record, the requester cannot decide, a refusal needs a reason, an approval runs the owning module's applier and links its correction entry, deciding twice writes nothing twice, a record that moved on makes the request stale). Found on the way and fixed in 0018 before the commit: `coalesce` cannot be schema-qualified, the decision reason clashed with the request's own `reason` column, and the shared IAM stamp trigger needs `created_by_user_id`, which this table does not carry. Step 3: SCR-192 as the approvals screen's second tab (`/approvals/revision-requests`, the two tabs through the shared `ScreenTabs`), old and new value side by side, the refusal dialog that asks for its reason, and the decision as a server action in the app layer, because carrying an approval out needs the appliers of the composition root. Step 4: migration 0019 (a stale request's event now names the requester too) and the TSK subscriber `tsk.revision-alerts` — a new request opens one high-priority task for its approvers at `/approvals/revision-requests` and tells each of them, the decision closes that task and tells the person who asked, with its own words for a request that went stale. Tests: 4 more real-database tests (one task per request, both approvers told, the task closed on a decision, the requester told, a second event changes nothing). Browser check with the owner signed in: the waiting request showed its fields, the refusal asked for a reason, and the refusal reached the database and the audit log. CI run 35797575645 green (check and database jobs). Carried over: who approves, how many steps and which notifications go out move into the flow designer in Phase 08; which record type locks when, and what a correction entry holds, come with each owning module's slice; the first real applier arrives with Phase 09 |
| TASK-0110 | Site-wide search foundation | T1 | IMPLEMENTING | TASK-0104 | D-251; REQ-NFR-012, ADR-017, D-247, D-248, `docs/ui-ux/SEARCH.md`; carries the build of TASK-0029. SPIKE-12: exact-match range rewrite, accent folding, cold first request exception. Plan written 2026-09-23 (D-266): four steps — migration 0020 (search row and its three scoped helpers), the platform search layer and `/api/search`, indexing from events with per-record-type projections in `src/records` plus a versioned rebuild, and the palette's record groups. Steps 1-2 built 2026-09-23: migration 0020 (`core.search_row` with the scope that decides who sees it, `core.search_posting`, `core.search_word`, Turkish folding through `core.search_words`, `core.can_see_record`, `core.index_search_row` / `remove_search_row` for the worker, `core.search_records` and `core.search_suggest` for the person, row level security on all three and `usage` on Supabase's `extensions` schema for pg_trgm), `platform/search` (query words, result groups, the projection contract, the `core.search-index` subscriber and the service) with its own data layer in `platform/db/search-store.ts`, and `/api/search`. ESLint learned two things: `platform/search` is its own data layer (its tables live in `core`), and the jobs registry may read the record registries. Tests: unit (folding, all-words, grouping and "Tümünü gör") and 8 real-database tests (site visibility, a commercial record hidden, both letter directions, every word in the same record, spelling corrected only from what the person may see, a removed record leaving no word behind, an older event not overwriting a newer index, direct write and remove). Step 3 built 2026-09-23: migration 0021 adds `core.search_word_bucket` — a word's records as sorted internal numbers, kept apart by record type, place and data class, with broad-view access checked per bucket and own-record access handled by source RLS, so a person reads only the buckets of their own places and classes (OQ-033, the owner's word). The buckets are kept with every write, not rebuilt from time to time, so the search intersects them before reading a row; a word with no bucket the person may read simply leaves the narrowing out, and the postings still decide, so a missing bucket can never hide a record. `npm run search:rebuild` builds them all again and `-- --check` reports disagreements without changing anything. Three more database tests: one bucket per place and class that the rebuild reproduces exactly, a bucket read only by people who may see what is in it, and a record that moves to another site taking its bucket with it. Step 4 started: palette reads record groups from the API, keeps local navigation available, cancels stale requests, handles loading/offline/error and retry; four async request tests added. 2026-09-23 continuation: type list destinations, revalidated session-only recent paths, phone fullscreen and source-owned paged rebuild implemented (0022/0023). Source rebuild stages and compares projections then publishes rows/helpers/version in one transaction; CLI queues the job. Search uses one DB call and six candidates per visible type; own-record fallback prevents another scope bucket from hiding a result. Clean installation/rollback extension issue fixed at ff5cfcb, CI 35805349980 green. Validation: 269 unit tests, build, full DB 158 tests then updated search suite 18 tests. 0024 uses policy-local IAM snapshots: palette SQL 629 to 45 ms, warm service p95 936 to 348 ms on 56 synthetic rows; 0025 combines transaction-local identity setup and the RLS palette query under a READ ONLY transaction with a pre-established 15s timeout, reducing four network trips to three. Same 56-row/20-sample warm p95 258 ms (max 259); 19 search DB tests and 273 unit tests pass. Production-scale gate remains open. 0027 adds normalization versions on rows/postings and posting projection versions, visible-only mismatch errors, rebuild/CLI metadata checks; 23 search tests pass including rollback/backfill; CI 35811145952 green; latest remote small-fixture p95 255 ms. OQ-034 awaits multi-scope ANY/ALL visibility decision. Real-source concurrency tests cover commit/rollback publication, update/move/delete/insertion behind cursor, duplicate delivery, source rollback and row/posting/vocabulary/bucket/version parity after queue drain; 9b6a9f9 passed CI 35830079315 including all 26 search tests after apply/down/up. Pre-publication catch-up implemented using a finite visible-event set difference, source re-projection and exact staged-count adjustment; late smaller IDs, post-cut normal delivery, partial catch-up failure, isolation rejection and scanned-row removal covered. Now 32 search tests defined, passed across full/targeted local runs; 273 unit tests and build pass. Initial CI 35832296803 exposed lexical event-ID pagination; qualified bigint ordering plus a digit/safe-integer regression fixed it (73116da). CI 35832578935 green: 172 DB tests including all 32 search tests before/after full rollback, two R2 tests skipped without credentials. Latest small-fixture p95 274 ms/max 278 ms. 0028 source-derived helper integrity implemented: worker-only invoker function shared by publication assertion and read-only CLI; full posting metadata/content, exact buckets and vocabulary counts compared with indexed text. All 51 search DB tests, 273 unit tests and build pass locally; CLI zero mismatches after cleanup; 7490e1c passed CI 35857129525 with all 191 DB tests including 51 search tests before/after full rollback (two credential-dependent R2 tests skipped). 0029 separates the helper generations: `core.search_word` and `core.search_word_bucket` carry `normalization_version` inside their primary keys, so two normalizers coexist without sharing a bucket or a count. Maintenance writes, recounts and evicts within the version in force; the read path narrows, joins postings and suggests only from that version, so a foreign generation can never narrow a record out of an answer; integrity compares buckets and vocabulary per version; the rebuild refreshes the vocabulary per version. Five new tests cover stamping, a foreign bucket not narrowing an answer, eviction on re-index, counts staying apart across generations, and a down/up round trip that leaves bucket contents and the integrity report identical. 56 search tests, 198 DB tests, 273 unit tests, format and build pass locally; 0b82633 passed CI 35862720223 with 196 DB tests including all 56 search tests before and after full rollback (two credential-dependent R2 tests skipped). 0030 adds the read-time check: for the records the palette returns (six per type at most) it asks whether each is in the bucket of every query word for its own type, scope, class and current normalizer, and returns one `helper_mismatch` flag for the answer; a record seen only as its owner is not judged, because that bucket is unreadable to them and its absence proves nothing. `core.search_records` keeps its shape, so no earlier migration or caller changes. The answer never changes and the flag never reaches the browser: the data layer writes one server-side line with no query text, title or id. Stated limit: a bucket that exists but has lost one id hides that record and cannot be seen this way; that direction stays with `core.search_integrity()` and the publication gate. Five more tests: healthy index silent, a deleted bucket flagged while the answer stays whole, an own-record hit not judged, a foreign-generation bucket ignored, and a down/up round trip leaving the answer identical. 61 search tests, 203 DB tests, 273 unit tests, format and build pass locally; warm smoke 279 ms p95 against 271 before, inside the 300 ms target; 7dc54c1 passed CI 35867217424 with 201 DB tests including all 61 search tests before and after full rollback. Production-volume gate measured 2026-09-23 with a 20,000-record fixture through the real request path: a common word took 942 ms and two of them 1,250 ms against the 300 ms of REQ-NFR-012. Five migrations answer it. 0031 returns a known word from the (word, record_type) index instead of comparing trigrams (385 ms to 48 at 2,000 records); 0032 loops only the kinds whose postings hold the words; 0033 replaces the per-kind call with one statement and a window that keeps six of each; 0034 counts the postings in a materialized CTE and reads only the holding rows by key, because the planner had been merge-joining the whole archive and running row level security on all 20,000 rows (718 of 790 ms); 0035 adds `ck_search_row__record_type_module` so the posting policy can read the module from its record type instead of looking up its row, and skips the kind-list query when every word is known. Measured again at 20,000 records, p95 with the round trip: 167, 287, 90, 212, 202 and 264 ms for the six sampled queries, all inside the target; about 260 bytes of index per record; a full integrity scan 1.5-2.4 s. Its own test caught a regression in 0035 — skipping the suggestion skipped the normalizer-version refusal with it, so the palette answered nothing instead of refusing; the check is now explicit and outside the fallback. 204 DB tests, 273 unit tests, format and build pass; 927b455 passed CI 35911823080 before and after full rollback. Rebuild cost measured and fixed the same day: a 20,000-record rebuild did not finish in ten minutes because the publication used the per-record writer, whose bucket append rewrites a bucket of three thousand ids for every id added and whose upkeep the global helper rebuild then discards. 0036 adds `core.index_search_row_bulk` for the publication path only — row and postings, no helpers — and the rebuild now takes 29 s at 20,000 records and 4.7 s at 2,000; b79c36a passed CI 35916332932. A record arriving on its own still uses `core.index_search_row`, so the index is never behind. OQ-034 answered 2026-09-23 (D-270): one scope is enough to find a record that several share, so multi-scope visibility can now be built. Multi-scope visibility built 2026-09-24 in 0037: `core.search_access_allows` judges each of a record's places on its own, so one is enough (D-270), and each place must grant both the view and the data class — half a grant on one place and half on another does not add up. The read-time helper check now names the bucket's own place, because a bucket lives under one place and judging it with "any place" called a healthy index damaged for anyone reaching the record through the other one. Four tests, two of which fail with the migration rolled back; a8f294e passed CI 35928366252. Still open: browser acceptance, a word shared by very many records (ranking reads every match before keeping six), rebuild cost at volume and the rest of production-scale query/rebuild acceptance including temporary event-set/full-check cost; first-slice modules will register real sources |
| TASK-0113 | Panel on the Home Screen and a bigger phone target area | T2 | IMPLEMENTING | TASK-0108 | Owner request 2026-09-23 (D-264): looking for "Paylas -> Ana Ekrana Ekle" takes too long, and instant notifications need the panel on the Home Screen anyway (D-252). Built: Home Screen icons drawn from the brand mark (`scripts/make-app-icons.mjs`, 192/512/maskable PNG plus `apple-touch-icon`), manifest and `appleWebApp` metadata, migration 0016 (`tsk.app_install`, `tsk.my_app_state`, `tsk.note_app_state`), `/api/app-install`, `InstallPrompt` on the entry screen (a window once at the first sign-in, then a strip on phones until it is done; Android and desktop Chrome install in one press through `beforeinstallprompt`, an iPhone gets the two steps because Apple offers no API). No follow-up list for the owner layer (owner 2026-09-23). Phone targets raised from 44 to 56 px in the bottom bar and the task list shows cards instead of a table under `md`. Waiting: an HTTPS address, because a browser offers installation only in a secure context. The owner declined a tunnel to the machine on 2026-09-24 (D-274), so the install and push acceptance waits for the hosting decided at the Phase 09 exit (DEF-008); everything else of this task is built. The push switch left the notification panel on 2026-09-24 (too loud, and it crowded the notifications above it) and now lives in the account menu, passed in by the layout because IAM may not import TSK. Owner's phone round 2026-09-23, all fixed the same day: the bottom bar's round action shrank to 36 px between 640 and 768 px (the button's own `sm:size-9` beat the shell's `size-16`); the header's search became an icon on a phone and is now the same box as on desktop, same height, with the trigger's keyboard shortcut shown only from `md` up and the placeholder truncated instead of spilling; hiding the palette's own footer with it was wrong and was put back the same day, COSS's default on every screen (owner); the modules drawer's tiles are COSS `Card`s on `bg-muted`, a shade apart from the drawer; the Home Screen label reads "Geoges Panel" instead of "GEOGES" (manifest `short_name` and the iOS title); the manifest, `sw.js` and `/assets` are public routes, because a phone reads them before anybody signs in; and the page itself no longer rubber-bands (`overscroll-none` on html and body, the page area contains its own scrolling), so the header and the bottom bar stay put on a screen with nothing to scroll. Part of these rode along in commit cad247d, whose message names only the revision screen |
| TASK-0111 | M1 local acceptance on the owner's machine | T2 | READY_FOR_IMPLEMENTATION | TASK-0110 | D-245, `docs/infrastructure/LOCAL_SETUP.md`: the owner installs and runs the panel, signs in with 2FA and walks a complete flow; feedback recorded. The walk itself is written down 2026-09-24 at the owner's request: `docs/features/m1-local-acceptance.md` — eleven steps in order, each saying what to do and what should be seen, and opening with what is deliberately absent so that an unbuilt slice is not written up as a fault. Feedback becomes tasks for fixes and change requests for anything that moves scope |
| TASK-0115 | CI's browser-side gate: accessibility scan, keyboard path and contrast test (Phase 09) | T2 | NOT_STARTED | TASK-0100 | `docs/infrastructure/CI.md` section 2 promises three GitHub checks that do not exist: the accessibility scan of every screen in both themes at both widths (D-225), the keyboard-path test (REQ-NFR-016) and the contrast test of the theme's colour pairs. They were recorded as arriving with the screens they test, and the screens are here. Found 2026-09-24 while walking the M1 list; raised as OQ-036 rather than either building it unasked or letting it stay a promise nobody tracks. All three need one thing first: a browser harness that signs in, which in CI means a test account and its secrets — the reason none of them was built. The contrast test in particular cannot be the unit test `ACCESSIBILITY.md` calls it: the tokens resolve through `color-mix`, `--alpha()` and the Tailwind palette, so the honest reading is the browser's computed value, which is also how TASK-0054 measured it by hand. Scope when it runs: the scan over every built screen at 375 px and desktop in both themes, one keyboard pass per screen pattern, and the pairs ACCESSIBILITY.md lists with their thresholds. Answered 2026-09-24 (D-276): it travels to Phase 09 and is built once against the address and test account the hosting decision brings, rather than twice. The accepted risk is written into D-276: until the harness exists a token change can break a threshold quietly |
| TASK-0116 | Schema test: the mandatory trio, per table layer | T2 | DONE | TASK-0101, TASK-0076 | `docs/infrastructure/CI.md` section 2 and `docs/database/COVERAGE.md` section 3: every table checked for a scope column, an RLS policy and a history channel. Measured against the 50 shipped tables on 2026-09-24 before being written, and the rule as recorded does not describe them: 11 of the 14 `business` tables carry no scope column and are right not to (a rate is company-wide, a document version hangs off its document, a notification belongs to a person), and two tables have no policy on purpose because the application has no privilege on them at all (D-272). OQ-037 answered 2026-09-24 (D-277): the rule is stated per layer and the scope source is declared in `core.table_layer` (`own` / `parent` / `person` / `company`), so the test reads a declaration the migration makes. Scope of the build: the migration that adds and backfills the declaration for the fifty existing tables, the registration check that requires it from every new table, the database test that enforces the trio per layer, and `COVERAGE.md` section 3 rewritten to the rule that now holds. Built 2026-09-24: migration 0044 adds `core.table_layer.scope_source` and declares it for all fifty tables (13 `own`, 9 `parent`, 7 `person`, 21 `company`); `scripts/db-layers.mjs` refuses a registered table with no declaration and one that claims `own` without carrying a scope column, and says nothing at all on a database where 0044 has not run, so a rollback stays clean; `scripts/schema-coverage.dbtest.mjs` measures the trio against the live database and proves it can fail with two throw-away tables that break it on purpose. Local: 308 unit tests, 234 database tests, the 0044 down/up round trip, format and build |

## PHASE 08 — Workflow Engine & Visual Designer

Entry: Phase 07's build work is finished; M1 is postponed by the owner and the phase is not closed (D-278). Shape decided 2026-09-24: D-279 (accepted against a probe module, the eight templates accepted in their own slices) and D-280 (the catalog is written with the five modules that exist).

| ID | Task | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0118 | Capability catalog and its contract test | T1 | DONE | TASK-0104, TASK-0102 | REQ-WFL-003, REQ-WFL-004, D-078, D-280. What each module can be asked to do, declared where the module is and read by the engine: actions, events, conditions, owner relations, with their data classes. The contract test of `docs/infrastructure/CI.md` compares the declaration with the code and fails when they drift — the check CI.md has promised since Phase 05 and could not have, for want of a catalog. IAM, ADM, AUD, DOC and TSK declare their real capabilities (open a task, notify, archive a document, start a revision request, read a dated rule); the ledger-finalising actions are not in any catalog, which is what makes REQ-WFL-002 a missing ability rather than a forbidden setting (D-080). Plan written 2026-09-24 (D-281 PROPOSED): the declaration carries the running function, so a declared capability with no implementation cannot compile, and the contract test checks the declaration against the module's REQ catalog, the declared events against the events actually published, and a snapshot that breaks CI when a capability disappears. Built 2026-09-24 (D-281 approved): `src/platform/capabilities` holds the format and `defineAction`, which parses with the declared schema before the module's own function sees anything; the five modules declare 25 capabilities (13 events, 1 action, 11 condition fields) and `src/records/capabilities.ts` joins them, because platform may not import a module. The test found four drifts on its first run — `exchange_rate.received`, `two_factor.reset`, `notification.created` and `notification.read` were published by the code and named in no catalog — and they were added to the records rather than dropped from the code (REQ-WFL-004). Only `notification.send` is declared as an action: opening a task as the flow needs the system-authority path that comes with TASK-0117, and declaring it earlier would be a promise the code cannot keep. 318 unit tests, lint, types, format and build pass |
| TASK-0117 | Workflow engine core: definitions, instances, execution | T1 | DONE | TASK-0118 | REQ-WFL-001…034, ADR-006, `docs/architecture/WORKFLOW_ENGINE.md`, D-235. Versioned JSON definitions and instances bound to the version they started on; the fourteen-step palette; the four triggers; the state machine on the existing job runner and outbox; conditions including the windowed ones with their time limit; the engine's own limits (500 steps, 500 list items, 50 concurrent instances, 180 days); dry run through the same evaluator with the business adapter out of reach; publish, supersede and disable. Accepted end to end against a probe record type (D-279); SPIKE-04…06 measured the risky parts and their carried-forward checks are the acceptance list. Step 1 built 2026-09-24: migration 0045 opens the `wfl` schema with the flow, its versions and the dry-run evidence, and puts the two rules that make a definition trustworthy in the database rather than in a screen — a published version's content cannot be changed by anyone, including the administrative connection, and a publish is refused without a passed dry run whose content hash is this definition's. One published version at a time is a unique index. 12 database tests, including a publish refused before the dry run, an edit that invalidates the evidence, supersession, the audit entry and the outbox event. Step 2 built 2026-09-24: migration 0046 adds the instance, its step states and the run log. An instance holds the version it started on, so a publish over a running job changes nothing; a single-instance flow keeps one run per record through a partial unique index rather than a rule; the same delivery never starts a second run; a loop that will not end is stopped by the step limit, and the limit is a recorded outcome rather than an exception, because an exception would roll back the log line that says why. Starting by hand is a second database door that asks for the design permission, since the engine's own door must not. 16 more database tests. Step 3 built 2026-09-24: the definition schema (the fourteen steps and nothing else, paths checked against the steps that exist), the engine's loop over start, condition and end, and the event trigger wired to the real outbox — publishing a definition writes the engine's subscription (migration 0047), so a flow starts listening the moment it is published and no registration has to be kept in step. A step the engine has not learned stops the instance with its name rather than being skipped. 9 unit tests for the schema and the condition, 8 database tests end to end including a real event through the outbox and a repeated delivery changing nothing. Step 4 built 2026-09-24: the approval step (migration 0048), where a flow stops and a person decides. Three outcomes and no fourth (D-099); a refusal or a send-back without a reason is refused by a check constraint, so no path can leave one behind (REQ-WFL-015); a decision is made once, by the person it belongs to. The flow is not resumed by the decision itself — deciding publishes `approval.decided` and the engine hears it like any other event, so neither half can lose the other. A step's owner is asked of the capability catalog's owner relations, which is how the engine finds the holder of a role without knowing that roles are IAM's. 3 more unit tests and 6 more database tests, including a send-back that opens the same step again. Step 5 built 2026-09-24: the task step, which is the first time the engine calls a capability action rather than writing something itself. `tsk.task` has carried `source_type = 'workflow'` and `source_step_run_id` since 0011, so migration 0049 only had to add the door the engine may use — worker-only, idempotent on the step visit — and put the step run into `task.completed`'s payload, so a waiting flow recognises its own task without reading TSK's rows. TSK declares `task.open` now that it can keep the promise. A real engine fault came out of it: running an instance that was sitting in an open step tried to enter that step again, which a repeated delivery did; an instance inside a step is waiting, and running it again now changes nothing. 4 more database tests. Step 6 built 2026-09-25: the dry run (REQ-WFL-025), and it is the same loop. What a step *does* — writing its state, opening an approval, asking somebody else to act — went behind a sink, and the dry run passes one that writes nothing and remembers everything; conditions are evaluated against the sample the designer gives and owners are worked out for real, so a branch is decided by data rather than by a schema check. SPIKE-05's finding is structural now: there is no second evaluator to drift. The engine writes its own evidence through a system door (migration 0050) rather than the person's, which asks for a permission the system does not have. A definition that will not parse is a failed dry run, not a crash. 7 more database tests, including one that asserts a dry run leaves no instance, no approval and no task. Step 7 built 2026-09-25: the notify and wait steps. Notifying is the catalog's own action, so the engine writes nobody's notification itself; waiting leaves a row in the scheduler rather than a timer in memory, which is what makes an eight-hour wait survive a restart, a deploy and a crash. The wake-up is keyed by the step visit, so a retry never schedules two, and a wake-up arriving twice finds the step already left. The dry run says how long the flow would sleep and schedules nothing, which a test asserts by counting the scheduler's rows before and after. 4 more database tests and 3 unit tests for the durations. Step 8 built 2026-09-25: the clock trigger (migration 0051). A flow driven by the clock says it in the platform's own words — every day at a time, or every N minutes — rather than in a cron dialect the panel would then have to own. What keeps a round that runs twice from starting two runs is the slot the run belongs to: "this flow, today" or "this flow, at 09:35", unique per flow, which is the clock's answer to what an event id does for an event. The round is a scheduled job every five minutes, which is also why a flow may not ask for a shorter period than that. 4 more database tests. Step 9 built 2026-09-25: an approval that waits too long moves on (migration 0052, REQ-IAM-020). The timer is set when the approval is opened and keyed by it, so a retried step never sets two; the person it moves to is worked out when the timer fires rather than when it was set, because eight hours is long enough for the role to be somebody else. The approval **moves**, it is not copied — one approval has one person who must answer it — and an approval answered before the timer fires is left alone, which is a decision beating a clock. The loop expresses the intent and each sink decides what it means, so the dry run reports when the approval would move and schedules nothing. 4 more database tests. Step 10 built 2026-09-25: the conditions that look back (D-100, SPIKE-06). A condition may count the flow's own history — how many times it has run for this record, how many approvals came back — within a window of days, and the count is read fresh every time: nothing is cached inside the instance, because a condition answering from an old count is one nobody can reason about. The query gets a limit of its own (two seconds, an engineering setting rather than a designer's) and the limit is restored afterwards, so one slow condition cannot spend the step's budget. Past the limit the database cancels and the flow **stops with the reason** rather than taking the "no" branch, which is what REQ-WFL-008 asks for. Migration 0053 also puts the step's own detail into the run log, so the number a condition counted can be read afterwards. 5 more database tests. Steps 11-14 built 2026-09-25, their records written the next morning after the session that built them was cut off: **the threshold trigger and the dependency lock** (migration 0054) — a threshold flow hears the event like anybody else and then asks whether the value it carries crosses the line, so there is no standing query behind it (D-103); a lock stops one transition and says why (REQ-WFL-029), only the owner layer or the general manager may pass it and only with a reason, which the audit log keeps (REQ-WFL-030, D-084). **Parallel paths and the join** (migration 0055): a branch is a child run of its own — its own log, its own waiting, its own step limit — and the parent carries on when the last one finishes, while a branch that fails stops the parent with the branch's own reason. **For each** (REQ-WFL-009): the list comes from the module that owns the records, one branch per item, an empty list is not a failure, a list past the engine's limit stops the run, and a for-each inside a for-each is refused by the definition (D-096). **Sub-flow** (migration 0056): another published flow started as a child, three levels deep at most, and an unpublished one stops the run rather than waiting for nothing. **Create a record / change its state** (D-095): the engine writes nothing itself — it calls the owning module's action and passes which flow, version and step is asking, so a module refusing to finalise a ledger refuses in its own words and the run stops with them (D-080). Step 15 built 2026-09-25 (D-282, the owner's answer to OQ-040): the escalation step tells somebody above and the flow carries on — a task and a notification through the catalog's actions, who it was raised to in the run log, and `next` continues, because a flow that stops for a warning is not a warning. **TASK-0117 is DONE**: four triggers, all fourteen steps of the palette, both kinds of condition, the engine's limits and the dry run, across twelve migrations (0045-0056) with their down files. The guard that stopped a flow at a step the engine had not learned now has nothing to catch, so the test that proved it became the invariant that keeps it idle: every step the palette offers is one the engine can take, which fails the day a fifteenth is added without being built. 69 database tests for the engine |
| TASK-0119 | Visual flow designer and its question-answer twin | T1 | IMPLEMENTING | TASK-0117 | REQ-WFL-026, D-085, `docs/ui-ux/ADMINISTRATION.md`, SPIKE-07. Both produce the same JSON and either can open what the other wrote; the palette is the fourteen steps and nothing else (REQ-WFL-005). Plan written 2026-09-25 now that the engine runs (D-283 PROPOSED, `docs/features/phase-08-workflow-plan.md`): one definition object edited by both, one schema validating it, the canvas library loaded only on the designer's route, the draft replaced in place through `wfl.save_draft`, and the keyboard and phone notes SPIKE-07 carried forward. Plan approved 2026-09-25 (D-283 ACCEPTED). Built the same day, step 1 of 2: the list (`/admin/workflows`, SCR-195's minimal columns), the designer route (`/admin/workflows/[flowKey]`) with its three states, `domain/graph.ts` — a pure definition-to-boxes-and-arrows function with 20 tests, so the layout can be read without a browser — the canvas itself (`ui/flow-canvas.tsx`, `@xyflow/react` fetched only when this screen opens, one tab stop with arrow keys, Enter into the panel, Escape back, mini-map off below `lg`), and the question-answer panel that edits the same object the canvas draws: a side panel on desktop, a bottom drawer on the phone. Error marks and the "Eksik var" badge come from `definitionSchema`'s own findings, attached to the step whose path the issue names; saving is automatic, 700 ms behind the typing, and a draft the schema refuses is not sent, because the database holds definitions to the same schema. Step 2 built the same day: the "+" on every arrow adds a step between two boxes (the palette is the fourteen of REQ-WFL-005 and nothing else), the panel asks every step's own questions, and the flow's own question — "Ne olunca başlasın?" — is what the panel shows when no step is selected. Every choice comes from the capability catalogs rather than from a list in the screen: the events a flow may start on, the fields a condition may read and the owner relations are the modules' declarations (REQ-WFL-003, D-280); the roles come from IAM's new `listRoles`, the people from `listPeople`, the other flows from `readFlows`. Adding and removing steps is `domain/edit.ts`, pure and tested (12 cases): a step taken out of the middle leaves the flow joined up, and a branch whose first step is gone is dropped. Step 3 finished the task's screen work the same day: **the dry run and the publish**. The dry run is asked for rather than run in the request (D-284) — the engine lives on the worker — so "Deneme çalıştır" schedules `wfl.dry_run`, the worker runs the engine's own loop with the sink that writes nothing, and the screen watches for the evidence row the database binds to the content hash. The report shows every step in order, what each one did and who it would fall to, and says plainly that nothing was created; it also says the sample record is empty until record types arrive (Faz 09R). "Yayımla" opens the confirmation REQ-WFL-024 asks for — which version goes live, which one moves to history, how many runs carry on where they are, that the publish is audited and the owners told — and it is offered only when a passed dry run of *this* definition exists, which the database enforces anyway. Four db tests cover the asking and the reading: one question per definition, an answer that knows whether it is stale, and nothing at all for somebody without the permission. Step 4: **"Yeni akış"** — one question, the name; the address is derived from it (Turkish letters flattened, a taken key numbered rather than overwritten, four tests) and the flow opens as a start and an end, which the schema accepts. Step 5: the header's **"…"** — a copy to work on (a draft of its own, the original untouched), closing the flow with a reason that is kept, and the version history with the reason it exists on the window. Step 6 came from the owner looking at it: the screen was speaking the code's language — step ids, capability codes, the schema's English messages and requirement numbers. Fixed at the source rather than patched per string: human step names in the domain (`stepNames`), one `Choice` component that gives COSS's select its items so a closed select shows a name instead of the stored value, a domain module that turns every schema finding into a Turkish sentence (with a sentence as its fallback, so English cannot leak), the engine's own refusals naming the step the designer named, the dry-run report carrying status and subject instead of engine words, the waiting time asked as a number and a unit, and no requirement numbers on any screen. IAM gained `listPermissions` next to `listRoles`. The canvas library's badge is off at the owner's request (MIT allows it; the badge is a request for support, not a condition). Screen work of SCR-195 and SCR-196 is complete; the quality gate is the owner's walk. Left for its own turn, and not pretended otherwise: choosing a sample record (needs record types, Faz 09R), dragging boxes by hand (D-283), and defining a new permission from inside the designer (REQ-WFL-021) |
| TASK-0120 | The approval centre's real queue and the eight templates | T1 | DESIGNING | TASK-0117 | REQ-WFL-012…016, REQ-WFL-028, D-086, D-099. The sample queue delivered by TASK-0037 is re-wired to the approvals the engine opens, with the three outcomes and the reason that a refusal or a return cannot do without; the eight end-to-end definitions of `docs/workflows/END_TO_END_FLOWS.md` ship as template JSON and each is activated and accepted in the slice that owns its records (D-279). Plan written 2026-09-25 now that the engine runs and the designer is built (D-285 PROPOSED, `docs/features/phase-08-workflow-plan.md`): the queue's one missing datum is **why this approval is with you** — the engine resolves the owner but records no rule, so migration 0057 puts the rule and its plain sentence beside the approval and lets an approval be addressed to a group, which is what an owner approval is; then the queue screen that fills one record at a time and opens the next without returning to the list, badge counts that agree in three places, migration 0058 for the template register with copy / reset / "a newer version exists", the default definitions as JSON seeds each checked in CI against the schema and walked in dry-run mode, and the run log SCR-197 where the queue's "why is this with me" continues. Step 1 built 2026-09-25 (migration 0057, up/down/up proved): the addressing rule travels with the approval, an approval may belong to a **group** — whoever holds a role or a permission, any one of whom answers — with one function behind the policy, the decision and the queue's read, and a delegate sees what the person they stand in for sees (company-wide delegations only for now, because an approval carries no scope yet). The queue's read is a definer function that answers only for approvals that are already the caller's, and carries the flow, the step and whether this run was sent back before. Five new database tests. It also fixed a latent bug: `request_approval` asked `if i is null` where it meant `if not found`, so a flow started by hand about no record was refused. Step 2 (2026-09-25): the queue screen itself — one record filling the screen and the next opening on a decision (D-070), the sentence that says why it is with this person (written from the rule and today's names, with a sentence as its fallback), three outcomes with a reason the window and the table both require, "sent back before" with the last reason, and the record's own screen honestly missing until the owning modules arrive. Migration 0058 adds whose approval it is to the queue's answer, so a delegate's card can name the person. The badge became the real number in three places: `workCounts` was a static empty map, the shell now takes counts as a prop, the layout reads them per person and "Bugün" asks the same question — that row is also the first real row on "Bugün", above the sample ones and without their label. Steps 3-4 (2026-09-25, migration 0059 + seed 0008, up/down/up proved): the template register, "Kopyasını kullan", "Şablona sıfırla" (a new draft, never a publish over what is live, audited) and the "Şablonun yeni sürümü var" badge — a template update tells a copy instead of changing it. Ten templates ship (the nine of REQ-WFL-028 plus the external-party sub-flow) and a database test parses each with the engine's schema and dry-runs it: nine walk end to end, personnel exit stops at a list no module has declared yet and the test asserts that with its reason (D-279). SCR-195 gained its "Şablonlar" tab; `templates` and `new` are reserved so a flow's address cannot shadow a tab. Next: the "Yeni akışlar" tab, the run log (SCR-197) and the eight end-to-end chains as templates |


## PHASE 15M — Panel MCP server (product surface)

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0114 | Panel MCP server — the panel answers in the person's own words | T1 | NOT_STARTED | Phases 09–15 module slices, TASK-0102, TASK-0103, TASK-0110 | ADR-019, D-267; D-268 PROPOSED (write surface), OQ-035 (KVKK basis, tied to OQ-024); REQ-NFR-004…005, REQ-INT-002/004, D-195, ADR-012. An MCP server inside the panel (`/api/mcp`) with no authority of its own: every call runs as the asking person through `runAsUser` and the same row level security, so the channel sees exactly what that person sees on screen. The tool surface is collected from each module's own registration, like search projections in `src/records`, which is why this sits after the slices — before them most of the surface does not exist. Reading: search, record detail, the management cards and reports the slices produce, tasks, notifications, approvals, revision requests; documents return metadata and a panel address only, never a signed storage link, and content is resolved to text by the server. Writing stays inside D-268: **drafts only**, nothing takes effect until the person opens the draft in the panel and confirms it; each draft carries an idempotency key and an audit entry marked `channel='mcp'`. Identity: a time-limited revocable token, a new `int.mcp.use` permission in the permission matrix, a KVKK access entry per call, and a critical alert on unusual reading volume with a daily cap as an administrator setting. Owed before the build: a requirement round for the channel, REQ-INT-002 narrowed to recommendation generation, RISK-001 split between development sessions and the product channel, an answer to OQ-035 |

## CHG-003 / CHG-004 — early UX and shell delivery

These deliver **Phase 02** (navigation, IA, role entry screen, top bar, mobile) and **Phase 07** (app shell) scope ahead of their phases, under approved change requests. Both phases are `PARTIALLY_DONE` in the roadmap.

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0040 | Re-review the CHG-004 shell work against the CHG-006 workflow decisions | T2 | DONE | TASK-0032, TASK-0033, TASK-0034, TASK-0035, TASK-0036, TASK-0037 | D-090. Owner asked (2026-09-18) that the five shell tasks awaiting review be re-evaluated in the light of the OQ-028 answers instead of approved as they stand. Produce a point-by-point list of what must change and present it: the sample approval queue must become the engine's real queue (D-088 puts the engine before the module slices), tasks must carry their "why was this opened" source (D-087), the work layer has to accommodate user-defined record types (D-079), and flow-design entry points belong only to full-visibility seats (D-083). Blocks the approval of TASK-0032…TASK-0037. **Result 2026-09-18 (presented to owner):** must change — (1) TASK-0037 approval queue has only Onayla / Düzeltme iste, D-099 needs Reddet as a third outcome; (2) approval records carry no "why is this with me" source (flow · step · record, D-087/D-097); (3) TASK-0037 task `source` is free text, must become flow · step · record with a link (D-087); (4) TASK-0033 sample notifications carry no source or link (D-087); (5) no "İş Akışları" entry exists — add it under Yönetim, guarded by a flow-design permission only full-visibility seats hold (D-083); (6) TASK-0032 `app-sidebar.tsx` fails Prettier on committed main. For later, no change now — registry, search and "Bugün" widgets are static, user-defined record types (D-093, Phase 09R) need dynamic entries (Phase 03 note); access policy has no "full visibility" notion yet (IAM, Phase 07); record search TASK-0029 must include user-defined types. Unaffected: TASK-0034 context row, TASK-0035 "Bugün", TASK-0036 mobile bar. **Owner decisions:** D-106 (remove the sample data instead of adapting it — items 1–4 become requirements of the real screens), D-107 (reason required on reject and send-back), D-108 (no "İş Akışları" entry now; item 5 decided later). Applied 2026-09-18: sample approvals, tasks and notifications deleted with their tests; empty states on both screens; bell without count; badges empty; mirrored "Bugün" figures zeroed; Prettier fix in `app-sidebar.tsx` (item 6). Verified: `npm run check` green (63 tests), and in the browser via a temporary signed-out probe (deleted, proxy change reverted): both empty states, empty bell, no badges, coordinator block empty without the sample label, 0 on the two figures, no console errors. TASK-0032…TASK-0037 are unblocked for owner review. |
| TASK-0028 | Functional page footer inside the app card (page-specific elements, e.g. primary/secondary actions, save bar) | T2 | DONE | Phase 02 screen specs | Owner request 2026-09-16: replaces the old logo/copyright footer inside the app card. Design per screen pattern in Phase 02 (list/detail/form standards REQ-NFR-013…015, sticky save "Kaydet ve yeni ekle"), then add an optional footer slot to the app shell. Design direction (owner): fixed to the bottom of the app card, layered above the scrolling content (bottom-navigation style), always visible while content scrolls; content must get bottom padding so nothing hides behind it. The old logo/copyright footer was removed from the app shell (a bottom brand strip was tried and removed at owner request, 2026-09-16). Designed 2026-09-20: `docs/ui-ux/SCREEN_PATTERNS.md` section 4, D-228 (confirmed by the owner 2026-09-20); built in Phase 07 as an optional bottom-band slot of the app shell. Implementation plan written 2026-09-23 (`docs/features/phase-07-foundation-plan.md`) with D-269 PROPOSED: a shell slot filled by a portal so server actions and client state share one band, a measured height written to the scrolling area for padding and scroll padding, the phone's bottom navigation hidden while the band is shown, no filtering of its own, COSS components only, applied first to the revision request decision, the task detail and the sample approval queue. D-269 ACCEPTED by the owner 2026-09-23. Built 2026-09-24: `platform/ui/app-shell/bottom-band.tsx` (provider, slot and band; the slot measures itself into `--bottom-band-height`, which the scrolling area uses for its bottom padding and its scroll padding), the shell wiring, the phone navigation stepping aside while a band is shown, and two screens — the task detail (closing steps in the band, reason field left with the record, buttons reaching their form through `form=`) and the "Görev ver" page (its action out of the frame footer; the dialog version keeps its own footer). Not applied, with reasons recorded: the approval queue has no action since its sample data was removed (D-106), and the revision request screen is a list whose cards each carry their own decision. Outside the shell the band renders its actions in place rather than swallowing them. 273 unit tests, lint, types, format and build pass. Browser pass done 2026-09-24 with the owner signed in: on the desktop the band sits at the card's bottom with the screen's action in it; the scrolling area carries the band's measured height as both padding (81 px = 57 + 24) and scroll padding (57 px), and a screen with no band keeps the slot hidden with the height at 0. At 375 px the band is 61 px at the bottom and the phone's navigation is out of the DOM entirely, returning on a screen without a band. The mechanism that carried the most risk was exercised for real: the band's button submitted the form it does not sit inside (`form=`), closing a task and leaving the reason field with the record. DONE |
| TASK-0030 | Development-only presentation sandbox (structure overview) | T3 | DONE | CHG-003 | D-052. Sections: module map with SVG connections (click a module), events, end-to-end flows REQ-WFL-011, REQ-WFL-028, roles & visibility (scope facts only; rest "Phase 01"), daily log approval cycle (REQ-FIN-015, REQ-SIT-001…004, REQ-SIT-006…008, REQ-SIT-013, REQ-SIT-030…032, REQ-WFL-015…016, D-035…D-040). Isolated: `src/sandbox/presentation`, CSS Modules, ESLint boundary, production 404, dev-only account-menu link. Branch `feature/presentation-sandbox`. 2026-09-16: all five sections written with lucide icons matching the left menu; check + build pass; isolation verified with a probe file (deleted); `/presentation` redirects to sign-in when signed out in development. Awaiting owner visual review — the page itself needs their account **Owner approved 2026-09-18.** |
| TASK-0031 | Navigation prototype in the sandbox (icon rail + header toolbar) | T3 | DONE | CHG-004, TASK-0030 | D-054…D-056. A development-only sandbox shell at `/sandbox/navigation`: two-region icon rail (work layer with badges, then module groups), three-zone header toolbar, context row that appears only when an object is open, and "Bugün" composed per role. Role switcher in the prototype so the owner can see the same skeleton as Sahip, Koordinatör, Saha Mühendisi and Taşeron Ekip Başı. No product code: `src/sandbox/navigation` + `src/app/(sandbox)`, production 404, COSS UI layer only (ESLint sandbox boundary). Flow methods that are still undecided (OQ-027) are shown as demonstrations and labelled as such. 2026-09-16: built and verified in the browser from all four role seats; `npm run check` (30 tests) and `npm run build` pass; signed-out visitors are redirected to sign-in; awaiting owner review 2026-09-17: deleted (`src/sandbox/navigation`, its route and the account-menu entry) — the product shell replaced it; the presentation sandbox (TASK-0030) is untouched |
| TASK-0032 | CHG-004 step 1: rail — work layer + group structure in the product sidebar | T2 | DONE | TASK-0031 | D-057…D-059. `navigation-registry.ts`: `workNavigation` (Bugün, Onaylar, Görevler), group icons, `sampleWorkCounts`, "Genel Bakış"/"Onay"/"Görevler" removed from groups, `allNavigationItems`. `app-sidebar.tsx`: collapsed rail with group flyouts (COSS `Menu`), expanded collapsible groups (COSS `Collapsible`), badge dot in icon state. `sidebar-group-preference.ts`: open groups cookie read on the server. 2026-09-16/17 verified in the browser via a temporary signed-out probe page (deleted): collapsed rail, flyout lists, two groups open together, open set survives reload, chevron rotation fixed (`in-data-[panel-open]`), badges 3/7 marked "örnek veri". 35 tests; check + build pass. Awaiting owner review  2026-09-17 refinements (D-068): first group open for a user with no preference, labels clipped instead of wrapping, logo cross-fade, card margin animated with the rail, separator margins removed (horizontal scrollbar). **Owner approved 2026-09-18.** |
| TASK-0033 | CHG-004 step 2: header as three zones (where you are / where you go / what you do), height stays 56px | T2 | DONE | TASK-0032 | D-061…D-063. `app-header.tsx` (three zones; grid keeps the search centred from `xl`), `command-palette.tsx` (COSS Command example layout, Turkish-insensitive filter in `search-text.ts`), `preview-roles.ts` (dev role switcher in the account menu), `site-scope-preference.ts`, `sample-notifications.ts`; registry gained `scope` and `primaryAction`. 2026-09-17 verified in the browser via a temporary signed-out probe (deleted): 1440px and 375px, palette search and Enter navigation, site choice remembered across pages, toast, notification list, role switch narrows rail/bell/sites. 45 tests; check + build pass. Record-level search (TASK-0029) still open. Awaiting owner review **Owner approved 2026-09-18.** |
| TASK-0034 | CHG-004 step 3: conditional context row (`contextBar` prop, absent when unused) | T2 | DONE | TASK-0033 | D-055, D-064. `context-bar.tsx` (generic row: title, section tabs, day strip, calendar), `modules/sit/ui/*` (sample sites, site context row, section placeholder, `Frame` list per D-060c), `platform/date/day.ts` (calendar days as `YYYY-MM-DD`, today in `Europe/Istanbul`), `(app)/@context/*` slot, `/sites` and `/sites/[siteId]/[[...section]]` routes, `contextBar` prop on the shell. 2026-09-17 verified in the browser via temporary signed-out probes (deleted): row renders at 44px with no scrollbars, section links and day links carry the right addresses, next-day disabled on today, calendar in Turkish with future days disabled and the chosen day selected, 375px without horizontal scroll, no console errors; a mirror probe proved the slot shows the row on a site address and nothing on `/dashboard` or `/sites`, both on full load and on client-side navigation. 54 tests; check + build pass. Awaiting owner review **Owner approved 2026-09-18.** |
| TASK-0035 | CHG-004 step 4: "Bugün" screen on `/dashboard`, composed per role | T2 | DONE | TASK-0033 | D-056, D-065…D-067. `today-overview.tsx` (work block + indicators side by side, fold for the rest, charts, site list), `today-work.ts` (per-seat sample work, site summaries, chart series), widget registry gained `critical`/`sampleValue`/`sampleUnit`, `platform/ui/chart/bar-chart.tsx`, `platform/ui/format/figure.tsx`; `cockpit-overview.tsx` removed. 2026-09-17 verified in the browser via a temporary signed-out probe (deleted) from all four seats, light and dark, 375px without horizontal scroll; 64 tests; check + build pass. Deviations 16a–16d. Awaiting owner review 2026-09-17 (owner): charts and the site summary removed from the screen; the chart component, the chart series, the site summaries and the two wide widgets were deleted with them, so the registry now holds only the figures. **Owner approved 2026-09-18.** |
| TASK-0036 | CHG-004 step 5: mobile — rail becomes a bottom bar below `md` | T2 | DONE | TASK-0032 | D-054, D-069, REQ-NFR-008. `mobile-bottom-bar.tsx` (work entries split around the primary action, modules in a `Drawer` with Turkish-insensitive search), `primary-action.tsx` (one implementation for header and bottom bar), header hides its menu button and action below `md`, context row scrolls the active section into view. 2026-09-17 verified in the real panel at 375px: bar renders under the content with safe-area padding, drawer opens, search folds Turkish letters, a module link navigates and closes the drawer, site detail shows header + context row + bottom bar together, desktop unaffected (bar hidden, menu button and action back in the header). 64 tests; check + build pass. Awaiting owner review **Owner approved 2026-09-18.** |
| TASK-0037 | Sample screens for "Onaylar" and "Görevler" (sample data removed 2026-09-18, D-106) | T2 | DONE | TASK-0035 | D-070. `modules/wfl/ui` (queue mode: one record, decide, next, "Bugün temiz" empty state), `modules/tsk/ui` (late / today / next, each task naming its source and its screen), own routes so the generated placeholders no longer answer for them. Sample data kept small (3 approvals, 1 task per state) and the figures on "Bugün" were aligned to these lists. 2026-09-17 verified in the real panel: queue advances to the empty state, tasks render in three sections, badges agree with the screens. 69 tests; check + build pass. Awaiting owner review **Owner approved 2026-09-18.** |
## PHASE 01 — Requirements & Domain Analysis (continued)

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0029 | Site-wide search (command palette) | T1 | DONE | Phase 01 REQ + permission matrix | D-044. Phase 01: REQ (searchable entities per module, result types, recent items, quick actions). Phase 02: UX with COSS `Command` (top bar trigger, keyboard shortcut, grouped results, empty/loading/no-permission states, mobile). Phase 03: architecture (permission-aware querying, data classification, search index vs. database search). T1 because results can expose commercial/sensitive data. Phase 02 UX written 2026-09-20: `docs/ui-ux/SEARCH.md`, D-227 (confirmed by the owner 2026-09-20); Phase 03 architecture in ADR-017 and D-247. DONE 2026-09-24: all three of its own deliverables exist and were owner-confirmed, and the build they describe was carried by TASK-0110, whose own remaining acceptance items (real module sources in a browser, ranking at much larger volume) are recorded there and belong to Phase 09 |
| TASK-0027 | Remove `docs/sources/` at Phase 01 exit | T3 | DONE | Phase 01 REQ coverage complete + TASK-0039 | **Blocked by CHG-005 (D-075):** the records cite `docs/sources/` by section number (REQ-FIN-015, REQ-NFR-008, REQ-SIT-003, REQ-SIT-030, REQ-WFL-011, REQ-WFL-028…) in hundreds of places. Deleting the file before those references are remapped to REQ ids would strip the reasoning out of the decision record. Unblocks only when TASK-0039 is DONE. Sources moved from repo root to `docs/sources/` on 2026-09-16 (owner request). Delete only after `ai/REQUIREMENTS.md` coverage is complete, architecture principles are captured in ADRs/`docs/architecture/`, and owner approves 2026-09-19: unblocked — TASK-0039 DONE and REQ coverage complete. 2026-09-19: architecture check done — 12 of 15 principles already carried by ADRs, decisions or requirements; all 15 and the layer model are now in `docs/architecture/PRINCIPLES.md`, with the three not yet decided in detail (single master record, input vs computed, report read models) bound to Phase 02/03/04. Remaining: owner approval, then removal. 2026-09-19: owner approved; `docs/sources/` (functional scope, architecture principles, original protocol) removed from the repository; text kept at Git tag `scope-archive`; every path reference repointed. |
| TASK-0044 | Requirement rounds and files for the remaining modules (all modules outside Slice 1, plus RPT REQ-RPT-015) | T1 | DONE | TASK-0021 | One question round per module, then `docs/requirements/REQ-<MODULE>.md` with its capability catalog (TASK-0041). 2026-09-18: TSK (13 requirements, D-130…D-133) and AUD (10 requirements, D-134, D-135) written, DRAFT. PRJ (11 requirements, D-136…D-138) written, DRAFT. ADM (15 requirements, D-139…D-141) written, DRAFT. TSK, AUD, PRJ, ADM confirmed by the owner 2026-09-18. Slice 2: INV (27), PUR (11), FAC (10) written, D-142…D-146, DRAFT. Confirmed by the owner 2026-09-18, incl. the derived transfer rule. FIN (30 requirements, D-147…D-154) written; confirmed by the owner 2026-09-18, incl. the D-154 derived rule; REQ-FIN-013, REQ-FIN-017 corrected for D-149. EQP (21 requirements, D-155…D-162) written, DRAFT; REQ-EQP-010, REQ-EQP-012, REQ-EQP-021 corrected; REQ-SIT-003 gains an equipment section. Confirmed by the owner 2026-09-18, incl. the D-158 derived rule. HR (16 requirements, D-163…D-168) written, DRAFT; RISK-011 opened. Confirmed by the owner 2026-09-18, incl. the D-164 derived rule. CRM (14 requirements, D-169…D-172) written; confirmed by the owner 2026-09-18, incl. the D-172 derived rule. QTE (18 requirements, D-173…D-176) written, DRAFT; REQ-QTE-005…006 corrected; REQ-CRM-014 clarified for product sales. Confirmed by the owner 2026-09-18, incl. the REQ-QTE-002 derived rule. CMP (17 requirements, D-177…D-180) written, DRAFT. Layer scan of all 328 requirements done (D-181): every requirement tagged Sabit / Akış / Tanım, validator enforces it. CMP and the scan confirmed by the owner 2026-09-19, incl. both CMP derived rules. QHS (16 requirements, D-182…D-186) written and confirmed by the owner 2026-09-19; REQ-HR-002/-003/-014 reworded for D-186. PRF (20 requirements, D-187…D-194) written, DRAFT; REQ-PRF-001…002, REQ-PRF-006, REQ-PRF-008…011, REQ-PRF-013 corrected; confirmed by the owner 2026-09-19, incl. both derived rules. INT (14 requirements, D-195…D-198) written and confirmed by the owner 2026-09-19. MTG (8), DOC (10), SUP (5) written, D-199…D-201, confirmed by the owner 2026-09-19 incl. both derived rules. STR (8 requirements, D-202…D-205) written; confirmed by the owner 2026-09-19 incl. both derived rules. RPT REQ-RPT-015 added as REQ-RPT-015…023 (D-206…D-208), confirmed by the owner 2026-09-19. NFR (20 requirements, D-209…D-212) written. NFR confirmed by the owner 2026-09-19: all module rounds done, 438 requirements CONFIRMED in 26 files (MIG deferred, DEF-001). Next: Phase 01 exit work (TASK-0039, OQ-007) |
| TASK-0045 | Role × module × data-class permission matrix | T1 | DONE | TASK-0044 | Phase 01 exit deliverable. Draft `docs/domain/PERMISSION_MATRIX.md` (15 default role templates × 25 module rows); four open cells answered by the owner (D-215). Confirmed by the owner 2026-09-19 |
| TASK-0046 | Domain model per module (entities, invariants) | T2 | DONE | TASK-0044 | Phase 01 exit deliverable. Derived from the confirmed REQ files: main records, their relations and the rules that must never break; events already live in each catalog. Written to `docs/domain/` 2026-09-19: `docs/domain/DOMAIN_MODEL.md` batch 1 (platform and first slice: IAM, AUD, DOC, WFL, TSK, ADM, PRJ, SIT, RPT) written; 17 structural record names added to the glossary as PROPOSED. Batch 1 confirmed by the owner 2026-09-19 with its 17 terms. Batch 2 (INV, PUR, FAC, FIN) confirmed with its 6 terms. Batch 3 confirmed with its 6 terms. Batch 4 (MTG, SUP, PRF, INT, STR) confirmed with its 3 terms: the domain model is complete (24 modules, 32 new record names in the glossary). |
| TASK-0047 | Confirm slice order and name pilot users | T2 | DONE | TASK-0045 | Phase 01 exit deliverable. D-048 runs the first pilot on sample data and names no pilot site or users; the owner confirms the slice order (roadmap phases 09–18) and who pilots each slice 2026-09-19 (D-216): order confirmed as planned; every slice pilots on sample data; real use starts at Phase 19; pilot site and users named before slice 1's build ends (Phase 09 exit condition). |
| TASK-0041 | Capability catalog for every module (events published, actions exposed, typed and classified condition fields) | T2 | DONE | — | CHG-006, D-078. Written per module alongside its requirement round (TASK-0021 and the later module rounds); the catalog is what the flow designer offers and what Phase 03 contract tests verify. A published capability is never removed, only deprecated 2026-09-18: catalog template written in `docs/requirements/README.md`; first catalog (WFL) at the end of `docs/requirements/REQ-WFL.md`. Remaining: every other module, written with its requirement round. 2026-09-19: every module file carries its catalog (NFR declares none by design; MIG deferred). |
| TASK-0043 | Align code names with the glossary: the "Bugün" screen and its indicators | T3 | DONE | — | 2026-09-21. Route `/dashboard` → `/today` with a permanent (308) redirect from the old address in `next.config.ts`; `dashboardRoute` → `todayRoute`; `platform/dashboard/dashboard-widget-registry.ts` → `platform/today/indicator-registry.ts` with `Indicator` / `indicatorRegistry`. Files moved with history. No behaviour change. Verified on the dev server: `/dashboard` 308 → `/today` 307 → `/sign-in` when signed out; no server errors; typecheck, lint, 97 tests pass. The signed-in Today screen was not opened (no password entry by the agent) |
| TASK-0039 | Remap every `docs/sources/` section reference to a REQ id | T2 | DONE | TASK-0020, TASK-0021 | D-075. Every `§N` citation in `/ai` and `/docs` is replaced by the REQ id that now carries that requirement; a reference map is kept in `docs/requirements/README.md`. `npm run records` then fails on any remaining `§` citation pointing at `docs/sources/`. Gate for TASK-0027 2026-09-19 (D-213): 835 citation chains in 41 files replaced by REQ ids; internal references (PROJECT_RULES, DESIGN_SYSTEM_RULES, ADRs, the original protocol) named explicitly; map of all 243 scope headings at the end of `docs/requirements/README.md`; scope kept at Git tag `scope-archive`; the validator now fails on any § that does not name its document. |
| TASK-0021 | Slice 1 requirement question rounds (IAM, SIT daily log, WFL approvals, RPT site detail + cockpit) and general OQ-021…OQ-024 | T1 | DONE | TASK-0020 | REQ-AUD-006, REQ-FIN-015, REQ-IAM-001, REQ-IAM-003…006, REQ-IAM-008…015, REQ-IAM-018, REQ-IAM-022…024, REQ-IAM-026…027, REQ-RPT-001…002, REQ-RPT-012…014, REQ-SIT-001…004, REQ-SIT-006…008, REQ-SIT-013, REQ-SIT-030…032, REQ-WFL-012…016 reviewed 2026-09-15; Round 1 answered (D-035 entry fallback order, D-036 email + password, D-037 deadline setting, D-038 holidays exempt / no-work log). Round 2 answered: no fixed default deadline (D-037 clarified), D-039 company vs personal email, D-040 delegate then escalation, all configurable by admins, D-041 owner delegate (OQ-023). Round 3 answered 2026-09-16: D-048 pilot on sample data (OQ-021), D-049 no target date (OQ-022), D-050 no KVKK legal review, RISK-001 stays open (OQ-024). Next: OQ-025 menu placement of Projects and CRM (explained to owner) 2026-09-18: REQ-WFL written (39 requirements, DRAFT) from OQ-028/CHG-006 and REQ-SIT-013, REQ-SIT-031…032, REQ-WFL-011…016, REQ-WFL-028, REQ-WFL-031 — no new question round needed, the WFL round was OQ-028. Remaining in Slice 1: IAM, SIT, RPT, each with its own round. 2026-09-18: IAM round done in two rounds (D-111…D-118), REQ-IAM written (27 requirements, DRAFT) with its capability catalog. Remaining in Slice 1: SIT, RPT. 2026-09-18: SIT round done in two rounds (D-119…D-125), REQ-SIT written (35 requirements, DRAFT) with its capability catalog. Remaining in Slice 1: RPT. 2026-09-18: RPT round done (D-126…D-129), REQ-RPT written (14 requirements, DRAFT; REQ-RPT-015 reports still to come). **Slice 1 requirement files complete: WFL, IAM, SIT, RPT — all DRAFT, awaiting owner confirmation.** **Owner confirmed all four files 2026-09-18.** |

## MILESTONE M0 — Early first screen (CHG-002, parallel to Phase 01)

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0022 | Implementation plan for M0 (structure, auth port, Supabase setup, tests, rollback) | T1 | DONE | CHG-002 | Plan written: `docs/features/m0-early-first-screen-plan.md` (Supabase docs checked 2026-09-16: `getClaims`, publishable key, PKCE `auth/confirm`, TOTP MFA, default email only to team + 2/hour). D-043 TOTP. Owner approved 2026-09-16 → DONE |
| TASK-0023 | Scaffold: Next.js 16 + TypeScript + Tailwind v4 + COSS UI, module folders, lint/format/boundary rules, test setup | T2 | DONE | TASK-0022 | 2026-09-16 on `feature/m0-early-first-screen`: Next.js 16.3.5 (create-next-app, merged into repo; Next agent block appended to `AGENTS.md`), React 19.2.8, Tailwind 4.3.3, `@coss/style` init (54 primitives in `src/components/ui`, CLI-managed), exact-pinned versions + lockfile, npm (provisional, OQ-017). Removed unused `radix-ui` and `cn` added by the init template (Base UI only, ADR-009). ESLint `boundaries/dependencies` (module→own module/platform/COSS; platform↛module) verified with probe files (2 expected errors, probes deleted). Vitest 5, Prettier + Tailwind plugin, scripts `typecheck/lint/test/format:check/check`. noindex via `robots.ts` + `X-Robots-Tag` verified at runtime. `.env.example` (names only). Self-review: vendored sidebar `react-hooks/purity` disabled for that file only (documented); ESLint 9.39.5 reports deprecation (tied to `eslint-config-next` 16.3.5) — revisit at Phase 07. All checks + build pass |
| TASK-0024 | Supabase dev project (EU Frankfurt) created by owner; public sign-up disabled; keys in local env by owner | T1 | DONE | TASK-0022 | 2026-09-16 owner created the project and wrote `.env.local`. Verified without reading key values: both variables present, key is `sb_publishable_` (not a secret key), `.env.local` git-ignored, public sign-up disabled and MFA (TOTP) enabled on the project's public settings endpoint. AI never handled secrets |
| TASK-0025 | Auth: sign-in, 2FA, password reset, session handling behind `AuthProvider` port with Supabase adapter | T1 | DONE | TASK-0023, TASK-0024 | Owner approved 2026-09-16 after testing real sign-in, password reset and two-factor authentication. 2026-09-16: `AuthProvider` port (`modules/iam/domain`) + Supabase adapter (`infrastructure/supabase`, provider error codes mapped to stable failure codes with Turkish messages); server actions for sign-in, sign-out, password reset request, new password, TOTP enrollment and verification; `platform/supabase/server-client.ts`; `src/proxy.ts` refreshes the session and redirects signed-out visitors; `getClaims` used for access decisions (never `getSession`); `aal2` gate in `(app)/layout.tsx` and on `/onboarding`; `/auth/confirm` route verifies `token_hash` with an in-app-only redirect target. Screens in the devl.dev design (D-046): sign-in, two-factor (verify + setup with QR), reset-password, update-password, header user menu with sign-out; onboarding restyled to match. 19 new unit tests (port contract with a fake adapter, routing rules, form validation). check + build pass. Browser-verified: signed-out visitor is redirected to `/sign-in`, wrong credentials show "E-posta veya parola hatalı." with the e-mail preserved, light/dark and mobile layouts, no page-load console errors. **Not yet verified (needs owner credentials):** real sign-in, TOTP setup and verification, password reset e-mail, onboarding screen's new look. Owner approval of auth rules pending |
| TASK-0026 | App shell (left navigation, top bar, light/dark, mobile) from registries + empty dashboard card skeleton + new-role onboarding screens | T2 | DONE | TASK-0023 | Owner approved 2026-09-16. 2026-09-16: `platform/access` (`AccessPolicy`, M0 preview policy), `platform/navigation` registry (REQ-NFR-007 groups, D-026 labels, permission per item, empty groups hidden), `platform/dashboard` widget registry (REQ-INV-026, REQ-RPT-002…004, REQ-RPT-006…009), app shell (COSS Sidebar, top bar, theme menu Açık/Koyu/Sistem, brand tokens in `platform/ui/theme/brand.css`), 25 auto-generated module placeholder pages, cockpit skeleton (`modules/rpt`), onboarding (`modules/iam`, static sample, step indicator), root layout `lang="tr"` + `latin-ext` fonts + noindex metadata. Server decides visible nav ids; client sidebar only renders (icons are not serializable). 9 unit tests; check + build pass; verified in browser (dark/light, desktop/mobile drawer closes on navigation, 0 console errors). Self-review gaps: vendored COSS sidebar has English screen-reader strings ("Toggle Sidebar", mobile "Sidebar"); COSS sidebar persists state via the Cookie Store API (browser support not verified); header user menu/sign-out comes with TASK-0025; menu lacks PRJ and CRM entries (OQ-025). 2026-09-16 brand logos added per owner (long in headers, stacked in footers, icon for collapsed sidebar + favicon; no apple-touch PNG yet). Awaiting owner visual review |
## PHASE 00 — Project Bootstrap & AI Infrastructure (continued)

| ID | Title | Tier | Status | Depends on | Notes |
|---|---|---|---|---|---|
| TASK-0011 | Initial commit and push to GitHub | T3 | DONE | TASK-0002…TASK-0010 | Commit `deb14ef` |
| TASK-0012 | Module map & dependency graph | T2 | DONE | TASK-0003 | Owner approved 2026-09-15 (draft; finalized in Phase 03) |
| TASK-0013 | UI/UX design-system, COSS and devl.dev rules | T2 | DONE | TASK-0003 | Owner approved 2026-09-15 |
| TASK-0014 | Glossary skeleton with proposed canonical terms | T2 | DONE | TASK-0003 | Skeleton delivered (Phase 00 exit criterion met); term confirmation carried into Phase 01 via OQ-007 2026-09-19: owner confirmed all 219 PROPOSED terms in four rounds of grouped questions (15 groups); 234 terms CONFIRMED, none PROPOSED or OPEN. |
| TASK-0015 | Identify project-specific skill needs | T3 | DONE | TASK-0007 | `ai/AI_SKILLS.md` |
| TASK-0016 | Initialize Tailwind docs snapshot locally (git-ignored) | T2 | DONE | TASK-0008 | Owner consent D-024; tailwindcss.com @ `7f92c22` |
| TASK-0017 | Impact analysis and decision for CHG-001 (early preview of auth + app shell) | T2 | DONE | TASK-0004 | Resolved: Milestone M1 in Phase 07 |
