# Phase 07 — Temel Yapım Planı

Durum: ONAYLANDI (sahip, 2026-09-21) · Tarih: 2026-09-21 · Son güncelleme: 2026-09-22 · Faz: Phase 07 · Bağlı: `ai/MASTER_ROADMAP.md`, CHG-009, D-250…D-253

Bu belge Phase 07'nin keşif ve planlama adımıdır (PROJECT_RULES §2, §3, §10). Hâlâ borçlu olunan temel kalemleri, her birinin tasarım kaynağını, doğrulama denemelerinden taşınan kuralları, yapım sırasını ve açılan görevleri listeler. Her T1/T2 görev, kod yazılmadan önce kendi uygulama planını (PROJECT_RULES §10) ayrıca yazar.

## 1. Zaten teslim edilenler

M0 ve CHG-004 ile erken teslim edildi: proje iskeleti, lint ve sınır kuralları, test düzeni (TASK-0023); Supabase ile gerçek giriş, TOTP iki adımlı doğrulama ve oturum koruması (TASK-0024, TASK-0025); uygulama kabuğu, gezinme kaydı, tema ve marka belirteçleri, rol karşılama (TASK-0026, TASK-0032…TASK-0036).

## 2. Soru turu — sonuç

Proje kuralının saydığı kategorilerin (iş gereksinimi, rol, senaryo, akış, UI/UX, veri modeli, yetki, kimlik, güvenlik, kenar durumlar, hata, entegrasyon, performans, ölçek, günlük, denetim, izleme, veri yaşam döngüsü, yedek, göç, test, erişilebilirlik, KVKK) hepsi Phase 01–05'te karara bağlanmış durumda; kaynakları aşağıdaki tabloda. Daha önce cevaplanmış soru ve yönetici ayarı olan konu tekrar sorulmadı. Gerçekten açık kalan üç konu 2026-09-21'de sahibe soruldu:

| Konu | Sahibin kararı | Kayıt |
|---|---|---|
| Self-host Supabase'e geçiş ne zaman kararlaştırılır | En sonda: Phase 19'da, gerçek veri girmeden önce | D-250 |
| Site geneli arama hangi fazda kurulur | Phase 07 temelinde; dilimler yalnız kendi kayıt türlerini ekler | D-251 |
| iPhone'da anlık bildirim yalnız ana ekrana eklenince çalışır | Kabul; rehbere ve ilk girişe "ana ekrana ekle" adımı konur | D-252 |

Teknik bir varsayılan da kapatıldı: paket yöneticisi ve çalışma ortamı npm 11 + Node 24 olarak kesinleşti (D-253, OQ-017).

## 3. Borçlu kalemler, kaynakları ve taşınan kurallar

| Kalem | Tasarım kaynağı | Denemelerden taşınan kurallar | Görev |
|---|---|---|---|
| Sınır kuralının MODULE_MAP'ten üretilmesi | MODULE_BOUNDARIES bölüm 2, ADR-001 | SPIKE-17: `dependencies` + `fileInternalPath`; grafik döngüsüzlüğü CI'da; statik SQL şema taraması | TASK-0099 |
| GitHub CI | CI.md, QUALITY_GATES | — | TASK-0100 |
| Veri erişim temeli: göçler, kısıtlı çalışma rolü, işlem yerel kimlik | ADR-015, CONVENTIONS.md | SPIKE-01/12: gerçek kısıtlı giriş rolü, parametre bağlama, işlem yerel kimlik ve hata sonrası temizlik, doğrulanan TLS; havuzda oturum `SET ROLE`'a güvenilmez | TASK-0101 |
| Sıfırlama komutları ve yapılandırma dışa/içe aktarma | D-246 | — | TASK-0076 |
| Dinamik yetki: kapsamlı roller, hiyerarşi, kişiye istisna, çoklu rol ve etkin rol, vekâlet, ayrılışta kapanma, veri sınıfı görünürlüğü, akış tasarımcısından tanımlanan yetki tipleri | PERMISSIONS.md, D-111…D-118, D-230, D-236, D-083, D-098, D-101 | SPIKE-01: dar yetkiler, kendine yetki verme kapalı | TASK-0102 |
| Denetim kaydı ve kayıt geçmişi | REQ-AUD, D-134, D-135 | — | TASK-0103 |
| Outbox, iş kuyruğu, zamanlayıcı ve okuma modeli yeniden kurma altyapısı | ADR-014, EVENT_BACKBONE.md, D-233, D-234 | SPIKE-03: kayıt başına sıra (baş seçimi), tekrar bastırma; SPIKE-14: kaynak kayıt başına satır, yalnız daha yeni olay günceller, gölge sürüm + atomik geçiş, karşılaştırma ve denetim, yalnız `replayable` aboneler | TASK-0104 |
| Kataloglar, tarihli kurallar, tipli özel alanlar | CONFIGURATION.md, D-237 | — | TASK-0105 |
| Kur ve iş günü takvimi | REQ-ADM-013…015, D-139, D-140 | SPIKE-11: tarihli dosya ve `Tarih` doğrulaması, `Unit` bölmesi, yerel saatle iş günü, tek `exchange_rate.missing`, bekleyen tutarların tamamlanması | TASK-0106 |
| Belge ve depolama | ADR-003, REQ-DOC | SPIKE-09: yetki denetimli kısa ömürlü bağlantı, sürdürmede yeni bağlantı; SPIKE-15: tarayıcıda küçültme, parçalı sürdürülebilir yükleme; SPIKE-16: yerel OCR iki geçiş, "okunuyor" işareti; SPIKE-10: PDF'te Geist `latin` + `latin-ext`, üst/altbilgiye ayrıca yazı tipi | TASK-0107 |
| Bildirim ve görev çekirdeği | REQ-TSK, D-130…D-133, ADR-018, D-252 | SPIKE-13: SSE sarmalayıcısı, veri isteğinde yeniden deneme ve `online` yenilemesi, sinyal yalnız tür taşır | TASK-0108 |
| Revizyon talebi çekirdeği | CONFIGURATION.md, REQ-ADM, D-141 | — | TASK-0109 |
| Site geneli arama temeli | REQ-NFR-012, ADR-017, D-247, D-248, `docs/ui-ux/SEARCH.md`, D-251 | SPIKE-12: tam eşleşme aralık yazımı; soğuk ilk istek istisnası; aksan katlama | TASK-0110 |
| Uygulama kabuğunda alt bant yuvası | SCREEN_PATTERNS.md | — | TASK-0028 |
| Kod adlarının sözlükle hizalanması, odak halkası ve Türkçe erişilebilir adlar | GLOSSARY, D-225 | — | TASK-0043, TASK-0054 |
| M1 — sahibin kendi makinesinde yerel kabul | D-245, LOCAL_SETUP.md | — | TASK-0111 |

## 4. Yapım sırası

Sıra bağımlılıktan gelir; her adım bir öncekinin üstüne kurulur.

1. **Zemin:** TASK-0099 (sınır kuralı), TASK-0100 (CI), TASK-0043 ve TASK-0054 (küçük hizalamalar).
2. **Veri erişimi:** TASK-0101, ardından TASK-0076.
3. **Kimlik ve yetki:** TASK-0102. Bundan sonraki her şey yetkiyi kullanır.
4. **Kayıt güvencesi:** TASK-0103 (denetim), TASK-0104 (outbox ve iş kuyruğu).
5. **Yapılandırma:** TASK-0105, TASK-0106.
6. **Hizmetler:** TASK-0107 (belge), TASK-0108 (bildirim ve görev), TASK-0109 (revizyon talebi), TASK-0110 (arama).
7. **Kabuk:** TASK-0028.
8. **Kabul:** TASK-0111 (M1).

## 5. Bu fazın dışında kalanlar

- Sunucu, dağıtım hattı, izleme ve uzak yedek; e-posta sağlayıcısı ve alan adı: DEF-008 → Phase 09 çıkışı. Phase 07'de e-posta kanalı yerel bir test posta kutusuna karşı kurulur.
- Self-host Supabase'e geçiş: D-250 → Phase 19, gerçek veriden önce.
- Çevrimdışı giriş: DEF-002 → ilk pilot geri bildiriminden sonra.
- Açık kalan uyum maddesi: R2 kovasının herkese açık `r2.dev` adresi gerçek dosyadan önce kapatılmalıdır (ADR-003); AB yargı bölgeli kova kararı gerçek personel dosyalarından önce verilir (D-249).

## 6. Kabul

Temel özellikler T1 kapısından geçer; M1 yerel kabulü sahibin geri bildirimiyle kaydedilir (D-245); barındırılan dağıtım ve geri dönüş Phase 09 pilotundan önce doğrulanır (DEF-008). Arama temelinin eklenmesiyle Phase 07 tahmini 8–11 iş gününden yaklaşık 10–13 iş gününe çıkar.

## 7. Görev uygulama planları

### TASK-0099 — Sınır kuralının MODULE_MAP'ten üretilmesi (T2)

- **Amaç:** bugünkü "modüller arası içe aktarım tamamen yasak" kuralını, MODULE_BOUNDARIES bölüm 2'nin öngördüğü biçime getirmek: bir modül başka modülü yalnız grafikte izinli bir okla ve yalnız `index.ts` üzerinden kullanır. İzinli oklar elle yazılmaz, `docs/architecture/MODULE_MAP.md`'den üretilir; grafik döngüsüzlüğü ve modüllerin başka modül şemasına SQL ile erişmemesi `npm run check` içinde denetlenir (SPIKE-17).
- **Bağımlılık:** yok.
- **Kural anlamı (MODULE_MAP'ten):** düz oklar doğrudan bağımlılıktır, geçişli değildir; kesikli oklar olaydır, kod bağımlılığı sayılmaz. Her iş modülü altı platform modülünü (IAM, AUD, DOC, WFL, TSK, ADM) kullanabilir. Platform modülleri iş modüllerine bağımlı olamaz.
- **Uygulama katmanı (`src/app`):** rotalar bileşimin köküdür. MODULE_BOUNDARIES kuralı modüller arasını bağlar; rotalar bir modülün `index.ts`'ini ve `ui/` ekranlarını kullanabilir, `application/`, `domain/`, `infrastructure/` gibi iç klasörlerine giremez.
- **Etkilenen dosyalar:** `eslint.config.mjs`; yeni `scripts/module-graph.mjs` (grafik okuma, döngü denetimi, politika üretimi) ve testi; yeni `scripts/check-schema-access.mjs` ve testi; kuralın gerçek yapılandırmayla sınandığı `scripts/boundaries.test.mjs`; `package.json` (`check` ve `check:commit`); yeni `src/modules/iam/index.ts`; IAM'in iç yollarını kullanan rotalar `@/modules/iam` üzerinden geçer.
- **Veritabanı / arayüz:** değişiklik yok. Davranış değişikliği yok; yalnız içe aktarım yolları.
- **Güvenlik:** veri sahipliği kuralını (MODULE_BOUNDARIES bölüm 1) makineyle korur.
- **Testler:** grafik okuyucu (gerçek MODULE_MAP: kenar sayısı, kesikli okların dışarıda kalması, platform grubunun tanınması, döngü yok; yapay döngünün yakalanması); gerçek ESLint yapılandırmasıyla sonda testleri (izinli ok + `index.ts` geçer; iç klasöre erişim, göreli yol, `import type`, dinamik içe aktarım, yeniden dışa aktarma, grafiğe aykırı yön ve platformdan iş modülüne erişim engellenir; rota `ui/` ve `index.ts` kullanabilir, iç klasöre giremez); şema taraması (başka modül şeması yakalanır, SQL olmayan dizge ve yorum sayılmaz); bütün repo temiz.
- **Göç:** yok. **Geri dönüş:** tek commit geri alınır.
- **Kabul:** `npm run check` bütün yeni denetimlerle geçer; sonda testleri kuralın gerçekten engellediğini gösterir; MODULE_MAP biçimi bozulursa kural sessizce gevşemek yerine hata verir.

### TASK-0100 — GitHub CI (T2)

- **Amaç:** yerel pre-commit kapısının aynısını her push ve pull request'te GitHub'da koşmak; kanca atlanarak yapılmış bir commit de `main`'de yeşil kalamaz (D-110, `docs/infrastructure/CI.md` bölüm 2).
- **Bağımlılık:** TASK-0099 (sınır denetimleri kapının parçası).
- **Etkilenen dosyalar:** yeni `.github/workflows/ci.yml`; `docs/infrastructure/CI.md`.
- **Kararlar:** Node 24 ve `npm ci` (D-253); kapı `npm run check:commit`; eylemler değişebilen etiketle değil commit özetiyle sabitlenir (RISK-004); iş yalnız okuma yetkisiyle çalışır; **tam git geçmişi** alınır, çünkü kayıt denetimi "Last updated" damgasını dosyanın son commit tarihiyle karşılaştırır ve silinmiş yolları arar; sığ klonda her dosya bugün değişmiş görünürdü.
- **Bu görevde olmayanlar:** CI.md bölüm 2'deki şema, sözleşme, erişilebilirlik, klavye ve kontrast testleri; bunların sınadığı şeyler (tablolar, yetenek kataloğu, ekranlar) ilgili görevlerle geldikçe eklenir.
- **Veritabanı / arayüz / güvenlik:** değişiklik yok; gizli anahtar kullanılmaz.
- **Test:** ilk push'ta iş yeşil biter (`gh run`); kapının GitHub'da gerçekten koştuğu ve tam geçmişle çalıştığı çıktıdan doğrulanır.
- **Geri dönüş:** iş dosyası silinir. **Kabul:** `main`'e her push'ta CI koşar ve geçer.

### TASK-0101 — Veri erişim temeli: göçler, kısıtlı çalışma rolü, işlem yerel kimlik (T1)

- **Amaç:** ADR-015'in Phase 07 koşullarını ürün koduna taşımak. Uygulama veritabanına yalnız kendi kısıtlı giriş rolüyle, doğrulanmış TLS ile bağlanır; kullanıcı isteğinden gelen her sorgu, kimliğin işlemin ilk ifadesinde yazıldığı bir işlemde çalışır; şema değişikliği sıralı, geri dönüşü yazılı SQL göçleriyle yapılır. Sonraki bütün görevler tablolarını bu temelin üstüne yazar.
- **Bağımlılık:** TASK-0100.
- **Kararlar (D-254):**
  - Sürücü `pg`, tipli sorgu kurucu **Kysely**. Kysely SQL'e yakın kalır, kod üretimi ve kendi şema dili yoktur; göçler ve RLS politikaları düz SQL olarak kalır (CONVENTIONS bölüm 10–11). Drizzle kendi şema dili ve göç aracıyla gelir; RLS politikası, sahiplik ve yetki ifadelerini yine elle yazmak gerekirdi.
  - Göç aracı küçük ve bizde: `db/migrations/NNNN_ad.sql` dosyaları sırayla, her biri tek işlemde uygulanır; uygulanan dosyanın adı ve SHA-256 özeti `core.schema_migration`'a yazılır; uygulanmış bir dosya değişirse araç durur. Eşzamanlı iki çalıştırmayı danışma kilidi (advisory lock) engeller. Her göçün yanında geri alma dosyası (`NNNN_ad.down.sql`) durur ya da dosyada `-- irreversible: <neden>` satırı bulunur; araç ikisinden biri yoksa çalışmaz (CONVENTIONS bölüm 11). `-- --rollback-last` son göçü geri alır; geri alınamayan göçün dönüşü göç öncesi dökümdür.
  - **Doğrulanmış TLS:** Supabase sunucu sertifikası kendi kök sertifikasıyla imzalı (Supabase Root 2021 CA); sistem sertifika deposuyla doğrulanamıyor (ölçüldü). Kök sertifika depoya konur (`db/certs/supabase-root-2021-ca.crt`, gizli değildir) ve her bağlantı `rejectUnauthorized: true` ile bu sertifikaya karşı doğrulanır. Sertifika bağlantı el sıkışmasından okundu; sahibin Supabase panelinden indirdiği sertifikanın parmak iziyle karşılaştırılması kayıt altına alınır. Self-host geçişinde (D-250) yol `DATABASE_CA_CERT_PATH` ile değişir. Doğrulamasız bağlantıya geri düşüş yoktur.
  - **Ayrı giriş rolü:** `geoges_app` — giriş yapabilir; süper kullanıcı değil, RLS'i atlayamaz, rol/veritabanı oluşturamaz, hiçbir role üye değildir; bağlantı sınırı var. Yetkiler her göçte tablo tablo verilir: `SELECT/INSERT/UPDATE`; `DELETE` hiçbir tabloda yok (CONVENTIONS bölüm 2); göç tablosu ve yetki tabloları yazılamaz. Uygulama sunucusu yönetici bağlantısını hiç bilmez: çalışma bağlantısı yalnız `DATABASE_APP_URL`. Parola `npm run db:app-role` ile üretilip `.env.local`'a eklenir (var olan satırlara dokunulmaz, hiçbir yere yazdırılmaz); aynı komut parolayı veritabanında ayarlar.
  - **İşlem yerel kimlik:** `core.current_user_id()` ve `core.current_role_id()` işlem yerel `app.user_id` / `app.role_id` değişkenlerini okur; değişken yoksa `NULL` döner ve RLS hiçbir satır göstermez. Değişkenler parametreyle `set_config(..., true)` ile yazılır; havuzlu oturumda `SET` / `SET ROLE` kullanılmaz (işlem havuzunda oturum durumu başka isteğe geçebilir).
  - **Zaman sıralı UUID:** PostgreSQL 17'de `uuidv7()` yok (ölçüldü); `core.uuid_v7()` SQL işlevi birincil anahtar varsayılanı olur (CONVENTIONS bölüm 2).
  - **Göç öncesi döküm:** `npm run db:backup` `pg_dump` 17 ile `backups/` klasörüne (Git dışı) döküm alır; **Değişti (D-255, sahip 2026-09-22):** döküm yalnız ortam gerçek veri ortamı olarak işaretlendikten sonra zorunludur; o andan sonra `db:migrate` son bir saatte alınmış döküm yoksa durur. Test döneminde döküm gerekmez; komut isteğe bağlı olarak kullanılabilir.
- **CI'da göç sınaması (CI.md bölüm 4):** GitHub'da ayrı bir `database` işi, bu çalıştırma için üretilen sertifikayla TLS açık geçici PostgreSQL 17 başlatır; göçleri uygular, ikinci çalıştırmanın boş geçtiğini doğrular, gerçek veritabanı testlerini koşar, göçleri tek tek geri alır, yeniden uygular ve testleri tekrarlar. Göç dökümü kuralından yalnız CI'nin bu makinedeki geçici veritabanı muaftır.
- **Etkilenen dosyalar:** yeni `db/migrations/0001_core_foundation.sql` ve `0001_core_foundation.down.sql`, `.github/workflows/ci.yml`, `vitest.db.config.mts`, `db/certs/supabase-root-2021-ca.crt`; yeni `scripts/db-migrate.mjs`, `scripts/db-backup.mjs`, `scripts/db-app-role.mjs` ve ortak `scripts/db-admin.mjs`; yeni `src/platform/db/` (bağlantı yapılandırması, havuz, `runAsUser`, kimlik doğrulaması, Kysely bağlama); `package.json` (bağımlılıklar ve komutlar); `.env.example`; `.gitignore` (`backups/`); `vitest.config.mts`; `scripts/check-schema-access.mjs` (ham SQL yalnız `modules/<kod>/data/` ve `src/platform/db/` içinde; `pg` yalnız `src/platform/db/` içinde).
- **Veritabanı (0001):** `core` şeması; `core.schema_migration`; `core.uuid_v7()`, `core.current_user_id()`, `core.current_role_id()`; `geoges_app`'e `core` kullanımı ve işlevleri çalıştırma yetkisi, başka hiçbir şey. `public` şemasında uygulama nesnesi yok; `anon` / `authenticated` (PostgREST) uygulama şemalarına erişemez. Ürün tablosu bu görevde yok.
- **Backend:** `runAsUser(identity, work)` havuzdan bağlantı alır → `BEGIN` → kimlik ve `statement_timeout` işlem yerel yazılır → iş, o bağlantıya bağlı Kysely işlemiyle çalışır → `COMMIT`. Hata olursa `ROLLBACK`; `ROLLBACK` da başarısız olursa bağlantı havuza geri verilmez, yok edilir (yarım işlem başka isteğe geçemez). Kimlik UUID olarak doğrulanır; havuz ve ham istemci dışa verilmez. Modül `server-only` işaretlidir. Kimliği doğrulanmış oturumdan üretmek TASK-0102'nin işi (kullanıcı ve rol tabloları orada); bu görevde yalnız sözleşme kurulur.
- **Frontend:** değişiklik yok.
- **Güvenlik:** ADR-015 koşullarının dördü de burada karşılanır. Sırlar yazdırılmaz; bağlantı hatası iletisi yalnız hata kodu olarak kaydedilir. Deneme yardımcısının kapalı sertifika doğrulaması ve geniş yetkileri ürüne kopyalanmaz. Servis bağlantısı yalnız göç, döküm ve rol komutlarında kullanılır.
- **Testler:** (1) Birim (CI'da): yapılandırma sertifikasız ya da doğrulamasız bağlantıyı reddeder; kimlik doğrulaması; `runAsUser` sahte istemciyle `BEGIN → set_config → iş → COMMIT` sırası, hata halinde `ROLLBACK`, `ROLLBACK` hatasında bağlantının yok edilmesi; göç aracının dosya sırası, özet uyuşmazlığı ve döküm kuralı; SQL ve `pg` konum denetimi. (2) Gerçek veritabanı (`npm run test:db`; yerelde Supabase test projesine, CI'da geçici veritabanına karşı): `geoges_app` havuz üzerinden doğrulanmış TLS ile bağlanır, yanlış sertifikayla bağlanamaz; rol öznitelikleri ve üyelikleri; geçici sınama şemasında RLS'li tabloyla kimliksiz işlemde sıfır satır ve yazma reddi, kimlikle yalnız kendi satırı; kimliğin sonraki işleme sızmadığı (eşzamanlı çok sayıda işlemde); hata veren işlemin geri alındığı; parametre olarak verilen SQL parçasının etkisiz kaldığı; `DELETE`, göç tablosuna yazma, `public` / `core`'da tablo oluşturma, `SET ROLE postgres` ve `auth.users` okumanın reddedildiği. Sınama şeması test sonunda kaldırılır.
- **Göç:** 0001 Supabase test projesine uygulanır (veritabanı ~12 MB; Free Plan sınırı 500 MB). **Geri dönüş:** `0001_core_foundation.down.sql` işlevleri kaldırır, şema yetkisini geri alır ve rolü siler (Supabase yönetici rolü `drop owned` kullanamadığı için yetkiler adıyla geri alınır); `core.schema_migration` araca aittir ve kalır. Kod commit'i geri alınır.
- **Kabul:** göç aracı 0001'i uygular ve ikinci çalıştırmada hiçbir şey yapmaz; uygulama rolü yalnız doğrulanmış TLS ile bağlanır; gerçek veritabanı testlerinin hepsi geçer; `npm run check` geçer; kalan tek sahip adımı (panel sertifikası parmak izi karşılaştırması ve PostgreSQL araçlarının kurulumu) kayıtlıdır.

### TASK-0076 — Sıfırlama komutları ve yapılandırmanın taşınması (T2)

- **Amaç:** D-246'yı kurmak: `db:reset:data` örnek iş verisini siler, yapılandırmayı korur; `db:reset:config` yapılandırmayı da fabrika ayarına döndürür ve onay ister; `config:export` / `config:import` yapılandırmayı dosyayla başka ortama taşır, çakışmayı raporlar, üzerine yazmaz; gerçek veri girildiği gün iki sıfırlama da kilitlenir (ENVIRONMENTS bölüm 4a–4b, LOCAL_SETUP bölüm 7).
- **Bağımlılık:** TASK-0101 (göç aracı, yönetici bağlantısı).
- **Temel fikir — her tablo katmanını söyler:** bugün ürün tablosu yok; tablolar sonraki görevlerle gelecek. Komutlar tablo adı ezberlemez: her tablo kendi göçünde `core.table_layer`'a katmanıyla kaydolur: `seed` (başlangıç verisi), `config` (yapılandırma), `business` (örnek iş verisi) ya da `system` (göç defteri gibi araç tabloları; hiçbir komut dokunmaz). Göç aracı her göçün sonunda, aynı işlem içinde denetler: uygulama şemalarında kaydı olmayan tablo varsa göç geri alınır. Böylece yeni bir tablo sıfırlamanın veya taşımanın dışında kalamaz.
- **Yön kuralı (yabancı anahtarlar):** `seed` yalnız `seed`'e, `config` `seed` ve `config`'e, `system` yalnız `system`'e başvurabilir; `business` her şeye başvurabilir. Aykırı başvuru göçü durdurur. Böylece iş verisi silinince yapılandırma kırılmaz ve dışa aktarılan yapılandırma iş kaydına bağlı kalmaz.
- **`db:reset:data`:** tek işlemde bütün `business` tablolarını boşaltır (`TRUNCATE … RESTART IDENTITY`), ardından başlangıç verisi dosyalarını yeniden çalıştırır. Yapılandırma durur.
- **`db:reset:config`:** önce yapılandırmayı otomatik olarak dışa aktarır (sahibin emeği dosyada kalır), sonra `business`, `config` ve `seed` tablolarını boşaltıp başlangıç verisini yeniden kurar. Onay ister: komut isteminde `SIFIRLA` yazılır; betikle çalıştırmada `--onay=SIFIRLA`.
- **Başlangıç ve örnek verisi:** başlangıç verisi `db/seeds/NNNN_ad.sql`, tekrar çalıştırılabilir (sabit kimlikli ekle-ya-da-güncelle); sabit kimlik şarttır, çünkü başka ortama taşınan yapılandırma bu satırlara kimlikle başvurur. `db:migrate` bekleyen göçlerden sonra başlangıç verisini de çalıştırır. Örnek veri `db/samples/NNNN_ad.sql`, `npm run db:sample` ile yüklenir. Bugün iki klasör de boş; dosyalar modüllerle gelir.
- **Kilit:** `core.environment` tek satırlık tablo; `real_data_started_at` doluysa iki sıfırlama ve örnek veri yükleme reddedilir. `npm run db:mark-real-data` işareti koyar (onay ister, geri alınmaz; canlıya geçiş adımıdır).
- **`config:export`:** `config` tablolarının bütün satırlarını tek JSON dosyasına yazar: biçim sürümü, tarih, veritabanının son uygulanmış göçü, tablo tablo satırlar (üretilmiş sütunlar hariç). Dosya `exports/` altına (Git dışı) ya da verilen yola yazılır. İş verisi ve arama yardımcıları girmez.
- **`config:import`:** hedefin son göçü dosyadakiyle aynı değilse durur (şemalar ayrışmıştır). Satırları birincil anahtarla karşılaştırır: hedefte yoksa eklenir; aynıysa atlanır; farklıysa çakışmadır. **Tek bir çakışma bile varsa hiçbir şey yazılmaz**, çakışmalar tablo ve anahtarla listelenir. Ekleme sırası yabancı anahtar bağımlılığından hesaplanır. `--dry-run` yalnız raporlar.
- **Etkilenen dosyalar:** yeni `db/migrations/0002_table_layers.sql` + `.down.sql`; `db/seeds/`, `db/samples/` (boş, `.gitkeep`); `scripts/db-migrate.mjs` (katman denetimi, göç sonrası başlangıç verisi); yeni `scripts/db-layers.mjs` (kayıt, yön kuralı, kilit, boşaltma, başlangıç verisi), `scripts/db-reset.mjs`, `scripts/db-sample.mjs`, `scripts/db-mark-real-data.mjs`, `scripts/config-transfer.mjs`; testler; `package.json`; `.gitignore` (`exports/`); `vitest.db.config.mts` (betik veritabanı testleri); ENVIRONMENTS, LOCAL_SETUP, CONVENTIONS (katman kaydı kuralı).
- **Veritabanı (0002):** `core.table_layer (schema_name, table_name, layer)` ve `core.environment`; ikisi ve `core.schema_migration` `system` olarak kaydolur. `geoges_app` bu tablolara erişemez; komutlar yönetici bağlantısıyla çalışır.
- **Backend / Frontend:** uygulama kodu değişmez; bunlar komut satırı araçlarıdır. Sahibin çalıştırdığı komutların iletileri Türkçedir.
- **Güvenlik:** komutlar yönetici bağlantısıyla ve doğrulanmış TLS ile çalışır; sır yazdırılmaz. Dışa aktarma dosyası kişisel veri içermemelidir; yapılandırma tablolarına kişisel veri konmaması katman seçiminin parçasıdır.
- **Testler:** birim — yön kuralı, bağımlılık sırası (döngü hatası dahil), çakışma karşılaştırması, onay ve dosya biçimi denetimi. Gerçek veritabanı (geçici sınama şemasında, katmanları kayıtlı tablolarla; sıfırlama ve taşıma işlevleri yalnız o şemaya sınırlanır, böylece test gerçek örnek veriye dokunmaz): kaydı olmayan tabloyla ve yasak yönlü başvuruyla göçün geri alındığı; `reset:data`'nın iş verisini silip yapılandırmayı tuttuğu ve başlangıç verisini geri kurduğu; `reset:config`'in önce dışa aktarıp sonra hepsini sıfırladığı; kilitliyken ikisinin de reddedildiği; dışa aktarılan dosyanın boş bir hedefe aynen yüklendiği, ikinci yüklemenin hepsini atladığı, farklı satırın çakışma olarak raporlanıp hiçbir şey yazılmadığı ve göç sürümü farklı hedefin reddedildiği.
- **Göç:** 0002 test projesine 2026-09-22'de uygulandı; D-255 gereği test döneminde göç öncesi döküm gerekmedi. **Geri dönüş:** `0002_table_layers.down.sql` iki tabloyu kaldırır.
- **Sonraki görevlere taşınan kurallar:** her yeni tablo katmanıyla kaydolur. Denetim ve kayıt geçmişi tablolarının (TASK-0103) katmanı ve sıfırlamada iş kaydı geçmişinin silinip yapılandırma geçmişinin korunması o görevin planında kararlaştırılır. Sıfırlanan örnek belgelerin R2'deki dosyaları TASK-0107'de temizlenir. Örnek kullanıcıların katmanı TASK-0102'de belirlenir; sahibin hesabı hiçbir sıfırlamada silinmez. `config:import` bir denetim kaydı bırakır (TASK-0103).
- **Kabul:** dört komut ve kilit çalışır; kayıtsız tablo içeren göç geçemez; testler CI'da geçer; `npm run check` geçer.

### TASK-0102 — Dinamik yetki: kapsamlı roller, hiyerarşi, istisnalar, etkin rol, vekâlet, görünürlük (T1)

- **Amaç:** PERMISSIONS.md'nin tamamını çalışır hâle getirmek: dinamik roller ve yetki tipleri, kapsamlı atamalar, süreli vekâlet, kişisel istisnalar, sahip katmanı, veri sınıfı izinleri, amir hiyerarşisi ve onay yedeği, işlemin hangi rolle yapıldığının kaydı. Yetki üç katmanda zorlanır (arayüz, sunucu, RLS); bu görev sunucu ve veritabanı katmanlarını kurar, arayüz kendi dilimlerinde bunu kullanır.
- **Bağımlılık:** TASK-0101 (veri erişimi), TASK-0076 (katman kaydı).
- **Kapsam dışı (kaydediliyor, düşürülmüyor):** "Kullanıcılar & Roller" yönetici ekranları ve akış tasarımcısı yüzeyi kendi dilimlerinde gelir; bu görev onların kullanacağı kayıtları ve servisi kurar. Kabuk bugün M0 önizleme politikasıyla (`platform/access`) çalışır; menünün gerçek yetkiye bağlanması, modüllerin kendi yetki tiplerini kaydettiği dilimlerde yapılır (CHG-002: gerçek veri gösterilmeden önce). İkinci adım (TOTP) bugün Supabase Auth'ta karşılanıyor (TASK-0025), ancak 2FA kurtarma kodları (D-236), hatalı girişte geçici kilit (REQ-IAM-005) ve 3 günlük hareketsizlikte oturum sonu (D-230) henüz kurulmadı. Bunlar Faz 07 kapsamındadır ve TASK-0112 olarak yapılır; ayarları tarihli kural, olayları denetim kaydı ve bildirim istediği için TASK-0103, TASK-0105 ve TASK-0108'den sonra gelir (D-257); bu görev yalnızca onların dayanacağı `iam.user` tablosunu kurar. Denetim kaydı yazımı TASK-0103'te bağlanır. Ayrılış tarihi gelen hesabın erişimi bu görevde kapanır; aynı gün giden bildirim ve oturum kapatma TASK-0104 (zamanlayıcı) ve TASK-0112 ile gelir. Akış motorunun sistem kimliğiyle `approvalOwner` çağırması Faz 08'de bağlanır.
- **Kararlar (D-256):**
  - **Hesaplar sıfırlanmaz, tanımlar sıfırlanır.** `iam.user` `system` katmanındadır: hiçbir sıfırlama gerçek hesabı silmez (D-134). Rol, yetki tipi, rol-yetki ve rol-veri sınıfı tabloları taşınabilir `config`'tir. Atama, kişisel istisna, elle amir ve rol seçimi de `config`'tir (`db:reset:config` ile fabrika ayarına döner) ama **taşınabilir değildir**: kişi adı taşırlar, başka ortamda kişi kimlikleri farklıdır. `core.table_layer`'a `portable` sütunu eklendi; `config:export` yalnız taşınabilir yapılandırmayı yazar. Taşınabilir bir tablo kişilere veya taşınamayan satırlara yabancı anahtarla bağlanamaz; iz sütunları (`created_by_user_id` …) bu yüzden yabancı anahtar değildir.
  - **Katman yön kuralı genişledi:** `seed` ve `config` artık `system`'e de başvurabilir (hesaplar hiç sıfırlanmadığı için güvenli). `system` yine yalnız `system`'e başvurur.
  - **Sahibin erişimi fabrika ayarından sonra kendiliğinden geri gelir.** `is_bootstrap_owner` işaretli kullanıcılar başlangıç verisiyle sahip rolüne yeniden atanır. İşareti `npm run iam:bootstrap-owner -- <e-posta>` koyar.
  - **Örnek kişiler** (`LOCAL_SETUP` bölüm 7) `iam.user.is_sample` işaretiyle `db/samples`'tan gelir; iki sıfırlama da onları ve onlara bağlı satırları (atama, amir, istisna) siler. Sahip ve örnek işareti bir arada olamaz.
  - **`iam.user.id`, Supabase Auth kullanıcı kimliğidir.** Doğrulanmış oturum satırı doğrudan gösterir; sağlayıcının kimliği ayrıca `auth_provider_id`'de durur (ADR-002).
  - **Etkin yetki veritabanında hesaplanır, sunucu istek başına bir kez okur.** `iam.my_grants()` / `iam.my_data_classes()` anlık görüntüyü verir; sunucu bunu istek boyunca önbelleğe alır ve önbellek istek bitince ölür, yani atama değişikliği ya da pasifleştirme bir sonraki istekte geçerlidir (REQ-IAM-006). RLS politikaları aynı işlevlerle yazılır; ikinci bir yetki dünyası yoktur.
  - **Tam görünürlük** (sahip katmanı dahil) her modülün bütün `view` yetkilerini ve bütün veri sınıflarını şirket genelinde verir (REQ-IAM-017); böyle bir rol yalnız tüm şirket kapsamıyla atanır. Sahip katmanının tuttuğu hiçbir yetki geri alınamaz, veri sınıfı kapatılamaz, ataması kısaltılamaz, hesabı panelden pasifleştirilemez, ona "kapat" istisnası yazılamaz (REQ-IAM-022…024).
  - **Kişisel istisna** bir yetki kodunu ya da bütün bir modülü hedefler: modül açma o modülün `view` yetkilerini açar, modül kapatma bütün yetkilerini kapatır. Kapatma her zaman şirket geneli, açma kapsamlı olabilir.
  - **Etkin rol** işlem yerel `app.role_id` ile taşınır. Kullanıcının o işleme izin veren tek rolü varsa sunucu onu seçer; birden fazlaysa ekran bir kez sorar ve seçim `iam.user_action_role_choice`'a yazılır. Veritabanı `iam.acting_role_valid()` ile kişinin taşımadığı bir rolün kaydedilmesini reddeder.
  - **Rol şablonları** PERMISSION_MATRIX'in hücre hücre karşılığıdır: her satır için modül düzeyinde `.manage` / `.view` / `.own` yetki tipi (Y / G / K), t ve h işaretleri rol-veri sınıfı satırı. Modüller kendi ince yetki tiplerini dilimlerinde ekler. Hiyerarşi varsayılanı: Sahip → Genel Müdür → Genel Koordinatör → Koordinatör → Saha Mühendisi → Formen → Taşeron Ekip Başı; Vinç Operatörü Saha Mühendisinin, ofis rolleri Genel Müdürün altında. Sahip hepsini değiştirebilir (REQ-IAM-009).
- **Etkilenen dosyalar:** yeni `db/migrations/0003_iam.sql` + `.down.sql`, `db/seeds/0001_iam_role_templates.sql`, `scripts/iam-bootstrap-owner.mjs`, `src/modules/iam/domain/permissions.ts` (+ test), `src/modules/iam/data/iam-store.ts` (+ `iam.dbtest.ts`), `src/modules/iam/application/access.ts`; değişen `src/modules/iam/index.ts`, `scripts/db-layers.mjs`, `scripts/db-reset.mjs`, `scripts/config-transfer.mjs`, `scripts/check-schema-access.mjs`, `eslint.config.mjs`, `package.json`.
- **Veritabanı (0003):** `iam` şeması ve dokuz tablo (`user`, `role`, `permission`, `role_permission`, `role_data_class`, `role_assignment`, `user_exception`, `user_manager`, `user_action_role_choice`); hepsinde RLS politikası, katman kaydı ve `geoges_app` yetkileri; `DELETE` yetkisi yok (geri alma `revoked_at` / `ends_on` ile). Kısıtlar ve tetikleyiciler: akış tasarlama yetkisi tam görünürlüğü olmayan role bağlanamaz ve böyle bir rolün tam görünürlüğü düşürülemez (D-083); sahip katmanı hiçbir yoldan daraltılamaz (IAM-K1, REQ-IAM-023); aynı kişi-rol-kapsam için tarih aralıkları çakışamaz (ortak tek bir şantiye/proje bile yeterli; tetikleyici, kişi başına kilitle); vekâlet bitiş tarihi zorunlu ve yalnız verenin bütün kapsam ve sürede taşıdığı rol için (REQ-IAM-018, REQ-IAM-019); rol hiyerarşisinde döngü yok; kapsam `company` ise `scope_ids` boş, değilse dolu. Koruyucular Türkçe mesaja çevrilen sabit ipuçlarıyla (`iam.owner_layer_unrestricted` …) hata verir.
- **Veritabanı işlevleri:** `iam.my_grants()`, `iam.my_data_classes()`, `iam.is_owner_layer()`, `iam.has_permission(code)`, `iam.has_company_scope(code)`, `iam.scope_ids(code, type)`, `iam.can_see(module, class, type, id)`, `iam.holds_role(role)`, `iam.acting_role_valid()`, `iam.my_account()`, `iam.manager_of(user, type, id)`, `iam.active_delegates(user, type, id)`, `iam.owner_users()`, `iam.is_active_user(user)`. Modül tablolarının politika kalıbı: `(select iam.has_company_scope('sit.module.view')) or site_id = any ((select iam.scope_ids('sit.module.view', 'site'))::uuid[])` — alt sorgular sorgu başına bir kez çalışır.
- **Backend (`src/modules/iam`):** `readEffectivePermissions()` (istek önbellekli), `assertCan(permission, scope)`, `visibleColumns(snapshot, module, columns, scope)` (izinsiz sütun sorgudan çıkar), `actingIdentity(permission, scope)` / `chooseActingRole(...)`, `managerOf(userId, scope)`, `approvalOwner(step)` (etkin vekil → asıl kişi → bekleme süresi geçerse ya da kimse kalmazsa üst rol; hazırlayan elenir; "sahip onayı" bütün sahiplere), `signInIdentity()`, `iamRuleMessage(error)`. SQL yalnız `modules/iam/data/`.
- **Frontend:** bu görevde ekran yok; giriş akışı ve kabuk değişmedi.
- **Güvenlik:** sunucu kontrolü atlanırsa RLS tutar; etkin yetki hesabı ve koruyucular tablo sahibi olarak çalışır, böylece kişinin kendi politikası bir denetimi gizleyemez. Yetki veren/geri alan hiçbir şey olay yayımlamaz (D-091).
- **Testler:** birim — kapsam, rol birleşimi, veri sınıfının modül ve kapsam bazında açılması, liste sorgusunda sütunun şirket geneli izin olmadan çıkması, etkin rol seçimi (tek rol sorulmaz, iki rolde bir kez sorulur, hatırlanan seçim geçersizse yeniden sorulur, istisna rol kaydetmez), onay yedeği sırası ve hazırlayanın elenmesi; katman kuralları (`system`'e başvuru, taşınabilir tablonun kişilere bağlanamaması). Gerçek veritabanı (23) — iki şantiyede RLS'in doğru satırları göstermesi, sahibin hepsini, tanınmayan ve ayrılmış kişinin hiçbirini görmemesi, matrisin karşılığı, ticari verinin şantiye bazında açılması, vekâletin bitişte kendiliğinden düşmesi, kişinin kendi rolünü devretmesi ve taşımadığı rolü/kapsamı devredememesi, akış tasarlama yetkisinin kısıtlı role bağlanamaması, sahip katmanını daraltan altı yolun reddi, çakışan atamanın reddi, tam görünürlüklü rolün şantiyeye atanamaması, istisnaların yalnız sahipçe yazılması ve modülü kapatıp açması, amir çözümü (aynı şantiyede üst rol, yoksa bir üst seviye, elle amir önce), etkin rolün doğrulanması, fabrika ayarından sonra sahibin rolünün geri gelmesi, örnek kişilerin bağlı satırlarıyla silinmesi, `config:export`'un atama ve kişi taşımaması.
- **Göç:** 0003 test projesine 2026-09-22'de uygulandı; geri alma dosyası da test projesinde çalıştırıldı. **Geri dönüş:** `0003_iam.down.sql` şemayı, katman kayıtlarını ve `portable` sütununu kaldırır; Supabase Auth hesapları durur, yeniden uygulamadan sonra `iam:bootstrap-owner` sahibi tekrar bağlar.
- **Kabul:** PERMISSIONS.md bölüm 2–5'teki her kural ya testle ya kısıtla karşılanır; `npm run check`, derleme ve CI veritabanı işi geçer; sahip katmanı hiçbir yoldan kısıtlanamaz.

### TASK-0103 — Denetim kaydı ve kayıt geçmişi (T1)

- **Amaç:** REQ-AUD-001…006 ve REQ-IAM-008'i çalışır hâle getirmek: her takip edilen tablonun alan bazlı değişiklik geçmişi (kim, ne zaman, eski ve yeni değer, neden, hangi rolle), yalnız eklenen şirket geneli denetim kaydı, geçmişte değerlerin veri sınıfına göre gizlenmesi ve sahiplerin "Denetim kayıtları" ekranı (SCR-193).
- **Bağımlılık:** TASK-0102 (yetki işlevleri, `iam` tabloları).
- **Kapsam dışı (kaydediliyor, düşürülmüyor):** her detay ekranındaki "İşlem geçmişi" bölümü ortak bir bileşen olarak ilk detay ekranıyla (dilim 1) gelir; bu görev onun okuyacağı `aud.history_of` işlevini ve sunucu servisini kurar. Onay, düzeltme isteği ve belge eklemenin geçmişte görünmesi (REQ-AUD-001'in bu üç parçası) o olayları üreten görevlerde bağlanır: akış motoru (Faz 08), revizyon talebi (TASK-0109), belgeler (TASK-0107). Başarısız giriş ve kilit olayları TASK-0112'de yazılır. Vekâletin bitiş tarihinde "vekâlet bitti" olayı zamanlayıcı ister ve TASK-0104 ile gelir. İptal (REQ-AUD-002) her modülün durum makinesidir; bu görev yalnız `DELETE`'in hiçbir uygulama rolünde olmamasını sınar. SCR-193 bu görevde liste kalıbının çekirdeğiyle gelir (başlık, süzgeçler, etkin süzgeç etiketleri, tablo / telefonda satır listesi, toplam kayıt ve sayfa numaraları); kayıtlı görünüm, yoğunluk, kart görünümü, süzgeçlerin hatırlanması ve süzgeçlerin `Sheet`/`Drawer` içinde açılması, ortak standart liste bileşeni Faz 09'da kurulduğunda bu ekrana da gelir.
- **Kararlar (D-258):**
  - **Geçmiş tek kanaldan, veritabanında yazılır.** Takip edilen her tabloya aynı `aud.capture_history()` tetikleyicisi bağlanır; eklemede tek satır, güncellemede değişen her alan için bir satır yazar. Kodun unutabileceği bir çağrı yoktur. Gerekçe `app.change_reason` işlem yerel ayarından gelir (`setChangeReason`); kişi ve rol `app.user_id` / `app.role_id`'den.
  - **Tablo kaydı geçmiş türünü de söyler:** `core.table_layer.history` = `tracked` (tetikleyici zorunlu, `id uuid` zorunlu), `append_only` (defterler ve denetim tabloları; güncelleme ve silme reddedilir) veya `none` (araç tabloları, tercih kayıtları). Göç aracı `tracked` olup tetikleyicisi olmayan tabloyu geri alır (CONVENTIONS bölüm 12'deki "geçmiş kanalına bağlı mı" denetimi).
  - **Sütunun veri sınıfı kayıtla bildirilir:** `core.column_data_class` (şema, tablo, sütun, `commercial` / `sensitive`), tabloyu oluşturan göçte yazılır; kaydı olmayan sütun `internal`'dır. Geçmiş satırı sınıfı yazıldığı anda taşır.
  - **Geçmişi kaydı görebilen görür:** geçmiş satırı kaydın modülünü, kapsamını (`site_id` / `project_id`) ve oluşturanını taşır. Satır, `<modül>.module.view` yetkisi o kapsamı kapsayan kişiye ya da `.own` yetkisiyle kaydın sahibine görünür. Ticari veya hassas alanın değeri izni olmayana gelmez; satır "değişti" olarak görünür (REQ-AUD-004). Okuma yalnız `aud.history_of()` üzerinden yapılır; tabloyu doğrudan okuma yetkisi yoktur.
  - **Denetim kaydı yalnız eklenir** (AUD-K1): uygulama rolünün yalnız `aud.record_event()` çağırma yetkisi vardır; güncelleme ve silme sahip dahil herkese veritabanı düzeyinde kapalıdır. Kişi ve yetki olaylarını (rol ataması ve bitişi, vekâlet, kişisel istisna, hesabın pasifleşmesi) `iam` tablolarındaki tetikleyiciler yazar; giriş ve çıkışı oturum kodu `iam.note_session()` ile yazar.
  - **Sıfırlama:** geçmiş kaydın katmanını izler. `db:reset:data` iş tablolarının geçmişini, `db:reset:config` ayrıca yapılandırma ve başlangıç verisi geçmişini siler (örnek veri döneminde; gerçek veri işaretinden sonra sıfırlama zaten kilitli). Denetim kaydı hiçbir sıfırlamada silinmez ve sıfırlamanın kendisini, içe aktarmayı ve dışa aktarmayı da kaydeder. Silme yalnız yönetici bağlantısında, sıfırlama işlemi içinde açılan bir izinle yapılabilir.
  - **Denetim ekranı** yalnız `aud.audit-log.view` yetkisiyle açılır (menü kaydındaki kod). Bu yetki yalnız sahip katmanındaki bir role bağlanabilir, kişisel istisnayla açılamaz ve tam görünürlüklü sahip dışı role kendiliğinden gelmez (REQ-AUD-006; veritabanı koruyucusu).
- **Etkilenen dosyalar:** yeni `db/migrations/0004_audit.sql` + `.down.sql`, `src/modules/aud/{domain,data,application,ui}`, `src/app/(app)/audit-log/page.tsx`; değişen `scripts/db-layers.mjs`, `scripts/db-reset.mjs`, `scripts/config-transfer.mjs`, `src/platform/db` (`setChangeReason`), `src/modules/iam` (giriş/çıkış olayı), MODULE_MAP (`AUD --> IAM`: denetim servisi yetkiyi IAM'e sorar; IAM denetime kod üzerinden değil, kendi veritabanı işlevleriyle yazar, böylece döngü olmaz).
- **Veritabanı (0004):** `aud` şeması; `aud.record_history` (`record_schema`, `record_table`, `record_id`, `operation`, `field`, `old_value`, `new_value` jsonb, `data_class`, `reason`, `site_id`, `project_id`, `record_created_by_user_id`, `changed_by_user_id`, `changed_in_role_id`, `changed_at`), `aud.audit_log` (`event_type`, `actor_user_id`, `actor_role_id`, `target_schema`, `target_table`, `target_id`, `payload`, `occurred_at`); ikisi de `system` ve `append_only`, RLS açık, doğrudan okuma/yazma yetkisi yok. `core.table_layer.history` ve `core.column_data_class`. `iam` tablolarına geçmiş tetikleyicisi ve olay tetikleyicileri. İşlevler: `aud.capture_history()`, `aud.record_event(...)`, `aud.history_of(schema, table, id)`, `aud.audit_log_page(süzgeçler, sayfa)`, `aud.purge_history_for_reset(tablolar)` (yalnız yönetici).
- **Backend (`src/modules/aud`):** `readRecordHistory(schema, table, id)` ve `readAuditLog(filters, page)` (yetki `assertCan('aud.audit-log.view')`), `recordEvent(type, target, payload)`; olay tipleri ve Türkçe adları tek listede. `platform/db`: `setChangeReason(db, reason)`.
- **Frontend:** SCR-193 `/audit-log` (kabuğun menü kaydındaki adres; SCREEN_INVENTORY'deki `/admin/audit-log` buna göre düzeltilir) — standart liste kalıbı (SCREEN_PATTERNS), COSS `Table`, süzgeçler: kişi, kayıt türü, zaman aralığı, işlem türü (REQ-AUD-006); boş durum "Bu süzgeçle kayıt yok", salt okunur (SCREEN_STATES). Menüde "Yönetim → Kayıtlar" altında, yalnız yetkisi olana.
- **Güvenlik:** geçmiş ve denetim tablolarına uygulama rolünün doğrudan erişimi yok; bütün yazma tetikleyici ve tanımlı işlevlerden, bütün okuma süzen işlevlerden geçer. Olay yükü hassas değer taşımaz; yalnız kimlik ve kod taşır.
- **Testler:** birim — olay adları, süzgeç doğrulaması, sayfa sınırları. Gerçek veritabanı — güncellemede yalnız değişen alanların yazılması, gerekçe ve rolün kaydı, doğrudan yazma/okumanın reddi, denetim satırının güncellenip silinememesi (yönetici bağlantısında da), şantiye kapsamına göre geçmiş görünürlüğü, ticari alanın izinsiz kişide "değişti" görünmesi, `iam` olaylarının denetime düşmesi, denetim sayfasının sahip dışına kapalı olması, `aud.module.view`'un sahip dışı role verilememesi, tetikleyicisiz `tracked` tablonun göçte reddi, sıfırlamanın geçmişi katmana göre silip denetim kaydını koruması.
- **Göç:** 0004 test projesine uygulandı; tarayıcı denetiminde ekranın yalnız kayıt türünü gösterip kimin kaydı olduğunu göstermediği görüldü, 0005 denetim sayfasına hedef kişinin adını ekledi (geri alma dosyası 0004 biçimine döner). **Geri dönüş:** `0004_audit.down.sql` `aud` şemasını, tetikleyicileri ve iki kayıt sütununu/tablosunu kaldırır (geçmiş ve denetim satırları gider; test döneminde kabul, D-255).
- **Kabul:** REQ-AUD-001…006'nın bu göreve düşen kabul ölçütleri testle karşılanır; `npm run check`, derleme ve CI veritabanı işi geçer; SCR-193 tarayıcıda sahip hesabıyla doğrulanır.

### TASK-0104 — Outbox, iş kuyruğu, zamanlayıcı ve okuma modeli yeniden kurma altyapısı (T1)

- **Amaç:** ADR-014 ve EVENT_BACKBONE'u çalışır hâle getirmek: olayın kaydıyla aynı işlemde yayımlanması, abone başına en az bir kez ve kayıt bazında sıralı teslim, tekrarın zararsızlığı, artan aralıklı yeniden deneme ve ölü mektup, zamanlanmış işler, okuma modellerinin gölge sürümle yeniden kurulması (D-233, D-234). İlk gerçek işler: ayrılış tarihi gelen hesabın kendiliğinden pasifleşmesi (REQ-IAM-007) ve vekâlet bitişinin olay olarak yazılması (TASK-0103'ten taşınan).
- **Bağımlılık:** TASK-0101 (veri erişimi), TASK-0103 (denetim kaydı).
- **Kapsam dışı (kaydediliyor, düşürülmüyor):** ölü mektup ve 15 dakikayı aşan gecikme için sahip katmanına kritik bildirim ve "sistem sorunu" görevi TASK-0108'de bağlanır; bu görev ikisini denetim kaydına yazar ve `npm run jobs:status` ile gösterir. Canlı sinyal kanalı (SSE, D-240) TASK-0108'dedir. İlk okuma modeli Faz 09'da (RPT) gelir; bu görev yeniden kurma altyapısını bir deneme modeliyle sınar. Ölü mektup ekranı (sistem gözü, RPT-011) Faz 15'e kadar komut satırındadır. Ayrılışta oturumların kapatılması TASK-0112'dedir.
- **Kararlar (D-259):**
  - **İşleyici uygulamayla aynı süreçte çalışır.** Next.js `instrumentation` kancası sunucu açılırken işleyiciyi başlatır (yalnız Node çalışma zamanında; `JOBS_WORKER=off` ile kapatılır). Ayrı bir komut gerekmez; M1'de sahip yalnız `npm run dev` çalıştırır. Birden çok süreç aynı anda çalışsa da güvenlidir: seçim satır kilidiyle ve kayıt bazında sırayla yapılır (SPIKE-03). Yük artarsa işleyici ayrı sürece taşınır; üreticiler değişmez (ADR-014).
  - **İşleyicinin kendi veritabanı rolü vardır:** `geoges_worker` (giriş yapar, RLS'i atlar, tablo ve rol oluşturamaz, silme ve boşaltma yetkisi yoktur). PORTS_AND_SERVICES bölüm 2'deki "servis bağlantısı yalnız göçler ve outbox işleyicisi için" kuralının karşılığıdır; yönetici bağlantısı uygulamada yine hiç okunmaz. Aboneler ve zamanlanmış işler bu rolle, kullanıcı kimliği olmadan çalışır (D-082: akış sistem yetkisiyle hareket eder); geçmiş ve denetim satırlarında kişi "Sistem" görünür. Parolası `npm run db:app-role` ile `DATABASE_WORKER_URL` olarak yazılır; bağlantı Supabase havuzunun oturum kapısından (5432) gider, çünkü işleyici uzun ömürlü bir istemcidir (işlem kapısı, rol yeniden kurulduğunda eski rol kimliğini önbellekte tuttu). Kayıtlı tablo taşıyan her şema bu role `usage` verir (göç aracı denetler); okuma modeli tabloları bu role `delete` verir.
  - **Yayımlama tek kapıdan:** `publishEvent(db, olay)` kullanıcının işleminde `core.publish_event()` işlevini çağırır; uygulama rolü outbox tablosuna doğrudan yazamaz. Veritabanı tetikleyicileri de aynı işlevi kullanır. İşlev aynı işlemde `core.event_subscription`'daki her abone için bir teslim satırı açar; abonelikleri işleyici kod içindeki kayıt defterinden yazar. Bir işleyici yalnız işleyicisini tanıdığı teslimleri ve işleri alır; böylece farklı kayıt defterli iki işleyici (örneğin testler ve açık geliştirme sunucusu) birbirinin işini almaz.
  - **Abone başına teslim, kayıt başına sıra:** bir teslim, aynı abonenin aynı kayıttaki daha eski teslimi bitmeden seçilmez (SPIKE-03'ün baş seçimi). Etki ile "teslim edildi" işareti aynı işlemde yazılır; `(olay, abone)` tekildir. Ölü mektuba düşen teslim o abonenin o kayıttaki sonraki olaylarını elle yeniden çalıştırılana kadar bekletir (sıra bozulmaz); diğer kayıtlar ve diğer aboneler etkilenmez.
  - **Deneme aralıkları** 1 dk, 5 dk, 15 dk, 1 sa, 6 sa; beşinci başarısızlıkta ölü mektup ve denetim olayı `system.dead_letter`.
  - **Zamanlanmış işler** `core.scheduled_job`'dadır; her çalıştırmanın tekrarsızlık anahtarı vardır (`tür:zaman`), aynı anahtar ikinci kez kaydedilemez. Tekrarlayan işler kayıt defterinde "her gün HH:MM (İstanbul)" ya da "her N dakika" olarak tanımlanır; işleyici sıradaki çalıştırmayı kendisi planlar.
  - **Kayıt defteri kod içindedir:** modüller abonelerini ve işlerini kendi `index.ts`'lerinden verir; `src/jobs/registry.ts` onları toplayan tek birleşim noktasıdır (sınır kuralında yeni `jobs` öğesi: yalnız platform ve modüllerin `index.ts`'ine erişir). Her abone `replayable` bayrağını taşır.
  - **Okuma modeli yeniden kurma:** okuma modeli kayıt defterine adı, tablosu, olay kodları, küme tabanlı `replay` ve `compare` işlevleriyle kaydolur. Yeniden kurma gölge tabloya yazar, yalnız `replayable` aboneleri çağırır, kaynakla karşılaştırır, farkı denetime yazar ve tek işlemde gölgeyi etkin tabloyla yer değiştirir; okuyan yarım model görmez (SPIKE-14). Satır kaynak kayıt başına tutulur ve yalnız daha yeni olayla güncellenir.
  - **IAM olayları outbox'a da yayımlanır:** yetenek kataloğundaki `role_assignment.created/ended`, `role_delegation.started/ended`, `user.deactivated` akışların tetikleyebilmesi için yayımlanır (D-091: yetki veren aksiyon yok, yalnız olay).
- **Etkilenen dosyalar:** yeni `db/migrations/0006_jobs.sql` + `.down.sql`; `src/platform/db` (`publishEvent`, `scheduleJob`, işleyici bağlantısı `runAsSystem`), `src/platform/jobs` (dağıtıcı, teslim döngüsü, zamanlayıcı, geri çekilme, okuma modeli yeniden kurma), `src/jobs/registry.ts`, `src/instrumentation.ts`, `src/modules/iam` (ayrılış ve vekâlet bitişi işleri), `scripts/db-app-role.mjs`, `scripts/jobs-cli.mjs` (`jobs:status`, `jobs:retry`, `jobs:rebuild`), `eslint.config.mjs`, `.env.example`.
- **Veritabanı (0006):** `core.outbox`, `core.event_subscription`, `core.outbox_delivery`, `core.dead_letter`, `core.scheduled_job`, `core.read_model`; hepsi `system` katmanında, `none` geçmişli (kendileri olay kaydıdır), outbox silinmez (D-231). `geoges_worker` rolü ve yetkileri (varsayılan yetkilerle sonraki tablolara da), `core.publish_event()`, `core.schedule_job()`. IAM tetikleyicisi outbox'a yayımlar.
- **Backend:** yukarıdaki platform parçaları; `iam`'de `deactivateDeparted` (her gün 00:05) ve `reportEndedDelegations` (her gün 00:10) işleri.
- **Frontend:** yok.
- **Güvenlik:** uygulama rolü outbox'a yalnız işlevle yazar, okuyamaz; işleyici rolü silemez, boşaltamaz, yapı değiştiremez; yük hassas değer taşımaz (kimlik ve kod).
- **Testler:** birim: geri çekilme takvimi, tekrarlayan işin sıradaki zamanı (İstanbul saati), kayıt defteri doğrulaması (aynı abone adı, bilinmeyen olay). Gerçek veritabanı (SPIKE-03'ün negatif testleri dahil): kaynak ile olayın birlikte geri alınması; aynı kaydın ilk teslimi kilitliyken ikincisinin seçilmemesi; teslim ile etkinin aynı işlemde olması (işlem ortasında hata → ikisi de yok); aynı olayın ikinci teslimi etki üretmez; bir abonenin hatası diğerini durdurmaz; beşinci hatada ölü mektup ve denetim olayı, sonraki olayın beklemesi, elle yeniden çalıştırmayla devam; zamanlanmış işin aynı anahtarla ikinci kez kaydedilmemesi; ayrılış tarihi gelen hesabın pasifleşmesi; yeniden kurmanın yalnız `replayable` aboneleri çağırması, gölge + tek işlemde geçiş, farkın denetime yazılması; işleyici rolünün silme ve RLS'siz okuma dışında yetkisizliği.
- **Göç:** 0006 test projesine uygulanır. **Geri dönüş:** `0006_jobs.down.sql` tabloları, işlevleri ve `geoges_worker` rolünü kaldırır; IAM tetikleyicisi 0004 hâline döner.
- **Kabul:** yukarıdaki testler yerelde ve CI'da geçer; `npm run check` ve derleme geçer; `npm run dev` ile işleyici başlar ve `jobs:status` kuyruğu gösterir.
