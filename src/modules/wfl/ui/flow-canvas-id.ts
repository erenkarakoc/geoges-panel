/**
 * The canvas element's id, in a module of its own so the panel can hand the focus back to the
 * canvas without importing the canvas — and with it the whole drawing library (SPIKE-07 note 3).
 */
export const flowCanvasId = "wfl-flow-canvas";
